# 009 — ELT v3: задачи

Порядок фаз обязателен: A (честный гейт) → B (непадающая петля) → C (watchdog) →
D (fleet-роли и контроль agy) → E (живой замер). Внутри фазы порядок указан явно.
`[P]` — можно параллелить (непересекающиеся файлы).

## Фаза A — гейт, который реально судит

- [X] **T001** Пофайловый дифф судье вместо слепой обрезки: `slurpDiff` отдаёт секции по файлам с бюджетом на файл и общим бюджетом (приоритет — тестовые файлы и файлы зоны `[files:]`), каждый обрезанный файл помечен явно `…(файл обрезан: показано N из M)…`, полностью невместившиеся перечислены отдельным списком «НЕ ПОКАЗАНЫ». `checkGrounding` спрашивает `filesReviewed` только по ПОКАЗАННЫМ файлам (невместившиеся не дают `unreviewed-file`), `phantom-file` остаётся безусловным. Тест: синтетический дифф 60K на 8 файлов — честный вердикт проходит, фантомный файл блокируется, обрезка видна в промпте. [files: tools/fleet/gate.js, tools/judge-grounding.test.js]

- [X] **T002** `elt judge run --task Txxx[,Tyyy]` — судья как шаг кода в интерактиве: спавнит `judge-invoke.js` (тот же путь, что драйвер: рубрика, двойной судья, red-proof), печатает вердикт и сам пишет proof через `writeJudgeProof`. `harness.json.judge.attest:true` → `elt judge-proof write` c ручным `--verdict` отвергается (exit 4) кроме `--skip-attest`, который пишет `attestSkipped:true` в run-log. Proof, написанный `judge run`, содержит `attested:true`; `validateJudgeProof` при `attest:true` требует это поле. Тесты: happy path пишет полный proof, ручной write отвергнут, `--skip-attest` проходит и оставляет след, `attest:false` = старое поведение. [files: ~/.claude/bin/elt.js, tools/judge-invoke.js, tools/elt-judge-attest.test.js]

- [X] **T003** Включить контур: `elt init` и `project-bootstrap apply` пишут `judge.verify` (провайдер, отличный от основного) + `redProof:"on"` + `judge.attest:true` в новый `harness.json`; включить в `.harness/harness.json` этого репо и двух живых проектов (`<another-project>`, `<another-project>`) — там же проверить, что `circuitEnabled()` = true. Обновить `/elt` SKILL.md: секция «Судья» = `elt judge run`, а не ручной `judge-proof write`. Тест: `elt init` в tmp-репо даёт конфиг с включённым контуром. [files: ~/.claude/bin/elt.js, ~/.claude/skills/elt/SKILL.md, .harness/harness.json, tools/elt-config.test.js, tools/project-bootstrap.js, <another-project> <another-project>/.harness/harness.json]

- [X] **T019** Внешний репо в промпте судьи — только зона, а не весь дифф: `slurpExternalDiffs` показывает файлы внешнего git-корня, попадающие в `[files:]` слайса, а не `git diff HEAD` целиком. Мотив — живой ложный block T003 (2026-07-24): слайс правил `~/.claude/skills/elt/SKILL.md`, а судья увидел заодно пользовательские `settings.json`, `plans/**`, `projects-registry.json` того же репо и вменил их как scope creep; любой слайс, трогающий `~/.claude`, блокируется чужим шумом. Файлы внешнего репо вне зоны — не показывать и не перечислять в grounding. Тест: внешний репо с файлом зоны + файлом вне зоны — в промпте только первый. [files: tools/fleet/gate.js, tools/fleet/gate.test.js]

## Фаза B — петля, которая не падает и думает

- [X] **T004** Park & continue вместо `break`: слайс, не прошедший гейт (red-stop / judge-block / judge-dead / пустой дифф), пишется в `.harness/parked.json` (`{tid, reason, ts, logPath, attempts}`), рабочее дерево откатывается (`git stash` с пометкой слайса), петля берёт следующий слайс. Стоп-условия остаются только жёсткие: STOP-файл, бюджет, `slice next` exit 3, approval/codegraph-guard. Итог прогона печатает закрытые и припаркованные раздельно и возвращает ненулевой exit при непустой парковке. `elt status` показывает секцию `parked`. Тест на PS-логике не нужен — тест на `elt status`/формате parked.json + DryRun-прогон. [files: tools/elt-loop.ps1, ~/.claude/bin/elt.js, tools/elt-parked.test.js]

- [X] **T005** Heal видит ошибку: `elt oracle` пишет хвост stdout/stderr прогона в `.harness/oracle-tail.log` (cap ~8K, перезаписывается каждым прогоном); драйвер кладёт в heal-промпт ИМЕННО его (плюс, отдельной секцией, хвост impl-лога), self-heal — до 2 попыток вместо 1, вторая с эффортом `max`. Тест: подставной красный оракул → в файле есть текст ошибки; тест на две попытки — через счётчик в run-log. [files: ~/.claude/bin/elt.js, tools/elt-loop.ps1, tools/elt-oracle-tail.test.js]

- [X] **T006** Промпт имплементатора v2 + эффорт по размеру: перед запретами — «сначала разберись» (codegraph_context по зоне задачи, спека и constitution, существующие тесты), затем решение, затем минимальный дифф; запреты остаются, но идут после. Слайс возвращает в конце JSON-заявку `{"filesChanged":[…],"testsAdded":[…]}` (готовит почву T009). Эффорт резолвится из тега сложности задачи (`[S]`→high, `[M]`→high, `[L]`→max) в `effort-policy.js`, драйвер передаёт тег. Тесты: `effortFor` по тегам, снапшот промпта содержит секцию разведки. [files: tools/elt-loop.ps1, tools/fleet/effort-policy.js, tools/fleet/effort-policy.test.js]

## Фаза C — watchdog

- [X] **T007** `[P]` `tools/harness-watch.js` — детекторы поверх run-log/ledger/health: `limit-streak` (≥2 limitHit одного провайдера за окно), `red-repeat` (≥2 red-stop по одной задаче), `judge-dead-streak` (≥2 подряд), `oracle-slow` (время оракула выше медианы ×3), `stale-park` (парковка старше N часов), `circuit-off` (в проекте с кодом контур выключен). Режимы `--once` (разовый прогон, exit 0/1) и `--watch` (поллинг файла). Пишет `.harness/health.jsonl` (по записи на инцидент, идемпотентно по ключу). Тесты на каждый детектор поверх синтетического run-log. [files: tools/harness-watch.js, tools/harness-watch.test.js]

- [X] **T008** Авто-фиксы из закрытого списка + проводка: `limit-streak` → провайдер в cooldown (`router.cool`) и следующий вызов идёт по цепочке; `red-repeat` → задача паркуется без третьей попытки; `judge-dead-streak` → судья откатывается на фолбэк-провайдера на остаток прогона; всё прочее — только запись. Каждое действие пишет `{action, reason, from, to}` в health.jsonl. Драйвер и fleet зовут watchdog между слайсами (`--once`) и уважают его решения В МЕРУ СВОЕЙ маршрутизации: у fleet есть цепочка провайдеров, поэтому `cooldown` применяется к воркеру (`router.cool`); у solo-драйвера имплементатор один (claude, цепочки нет), поэтому он применяет `cooldown` к провайдеру СУДЬИ и пишет явный `watchdog-cooldown-noop`, если решение неприменимо. Перевод имплементатора драйвера по цепочке — это T010 (failover воркера), не этот слайс. Подтверждать (`ack`) можно только фактически применённое действие. Никаких действий, трогающих код или git. Плюс недостающая проводка времени оракула: `elt oracle`/`commit` пишут в run-log `oracle.durationSec` (сейчас длительность измеряется и печатается, но НЕ сохраняется — детектор `oracle-slow` из T007 мёртв на живых данных). Тесты: каждое действие применяется ровно раз, неизвестный класс не даёт действия, зелёный `elt oracle` оставляет в run-log запись с `oracle.durationSec`. [files: tools/harness-watch.js, tools/elt-loop.ps1, tools/fleet/fleet.js, tools/harness-watch.test.js, tools/elt.js, ~/.claude/bin/elt.js]

## Фаза D — fleet-роли и механический контроль agy

- [X] **T009** Worker-attestation (контроль галлюцинаций воркера, 0 LLM-вызовов): воркер обязан закончить JSON `{"filesChanged":[…],"testsAdded":[…]}`; `tools/fleet/attest.js` сверяет заявку с реальным диффом worktree — заявленный, но неизменённый файл → `hallucinated-file`, изменённый незаявленный → `undeclared-file`, пустой дифф при непустой заявке → `phantom-work`, нет JSON вовсе → `no-attestation`. Любое расхождение: слайс НЕ идёт к судье, пишется в ledger и перевыдаётся следующему провайдеру цепочки. `workerPrompt` дополнен требованием заявки. Тесты на четыре отказа + честную заявку. [files: tools/fleet/attest.js, tools/fleet/attest.test.js, tools/fleet/fleet.js, tools/fleet/fleet.test.js, tools/harness-watch.test.js]

  Зона расширена на тесты со стабами воркера (`fleet.test.js`, `harness-watch.test.js`;
  уточнение границы, утверждено 28.07): контракт воркера
  стал обязательным, поэтому стабы воркеров существующих fleet-тестов обязаны его соблюдать —
  иначе attest честно режет их как `no-attestation`.

- [X] **T010** Failover воркера и роли (сюда же — цепочка провайдеров имплементатора для solo-драйвера, вынесенная из T008): `implement`-вызов, упавший по limit/timeout/fatal-config, перевыдаётся следующему провайдеру цепочки (переиспользовать `router.failover`, а не новый механизм), с записью `failoverFrom` в ledger и лимитом попыток `maxAttempts`; воркеры больше не бегут `lean` по умолчанию (`FLEET_LEAN=1` — явный откат); судья слайса не может совпасть с провайдером его воркера ни первичным, ни `verify` (резолв в коде: agy-воркер → судьи claude/codex); red-proof обязателен и в fleet-гейте. Тесты: failover по лимиту доводит слайс до гейта, agy-воркер не получает agy-судью, lean выключен по умолчанию, зелёный red-proof режет fleet-гейт, мёртвый verify-судья перевыдаётся живому CLI (и не воркеру); `judge-verify.test.js` в зоне по той же причине — его кейс «второй судья мёртв» теперь значит «мёртв И заменить некем», и незастабленный CLI считался бы живой альтернативой. Зона расширена (утверждено юзером 2026-07-28): под контуром (`redProof`+`judge.attest`) fleet-гейт вообще не мог закоммитить — proof шёл без `judges/grounding/redProof` и без пометки происхождения, `elt commit` его законно отвергал; нужен маркер машинного источника у `judge-proof write` (`--attested-by fleet-gate`, громкая строка в run-log). Плюс замер на самом слайсе: verify-судья (codex/gpt-5.6) трижды не ответил на диффе этого слайса (301/301/540с) — второй слой контура молча выключался, и слайс было нечем закрыть. Замер развёл ДВЕ причины, которые сперва казались одной: codex ВИСИТ на крупном промпте (301/301/540/300с — поднятие лимита до 9 мин ничего не изменило, а на коротком промпте тот же CLI отвечает за секунды), а claude/sonnet на том же диффе отвечает за 265с, то есть в 5 минут упирается ВПРИТЫК и на чуть большем диффе уже не успевает (302с). Поэтому лечится и то, и другое: `JUDGE_TIMEOUT_MS` 5→8 мин (живому судье хватает с запасом, зависший всё равно упрётся), И мёртвый verify-судья перевыдаётся следующему ДОСТУПНОМУ CLI (тот же failover-мотив, что у implement-вызовов выше), не совпадающему ни с первичным судьёй, ни с воркером слайса; мёртвая попытка остаётся в `judges[]` — след в proof, а не тихая подмена (утверждено юзером 2026-07-28). Следствие того же решения, найденное живым гейтом: валидатор proof в `elt.js` требовал вердикт `pass|block` от КАЖДОЙ записи `judges[]` и отвергал перевыдачу как `missing-judges` — то есть гейт с failover не мог закоммититься вовсе; теперь вердикт спрашивается с ОТВЕТИВШИХ судей (`runOk!==false`), и хотя бы один ответивший обязателен, иначе proof из одних смертей проехал бы как pass. **Корень «зависаний» codex найден разбором живого лога (567KB, 2026-07-28):** он не висел и не ждал апрува (`approval: never`) — он спавнился с `--sandbox workspace-write` и уходил ГОНЯТЬ ОРАКУЛ внутри судейского вызова (`exec powershell -Command 'node tools/elt-oracle-runner.js'`), то есть агентный CLI с правом записи вёл себя как исполнитель: не укладывался ни в какой таймаут (300/301/301/481/540с, DEAD 5 из 5) и писал в дерево, попутно порождая `stale-tree`. Роль судьи ограничена структурно: `providers.run({readOnly:true})` из `judgeDiff` → codex-судья спавнится `--sandbox read-only` (воркеру запись остаётся), плюс явный запрет запускать команды в самом промпте судьи. [files: tools/fleet/fleet.js, tools/fleet/gate.js, tools/fleet/providers.js, tools/fleet/fleet.test.js, tools/elt.js, ~/.claude/bin/elt.js, tools/elt-judge-attest.test.js, tools/fleet/providers.test.js, tools/judge-verify.test.js]

- [X] **T014** Информационный контур судьи — бюджет диффа и сверка внешних путей (обе причины найдены живым гейтом T010 2026-07-29, обе про то, ЧТО судья видит и КАК сверяется; правка вынесена из T010 отдельной задачей намеренно: имплементатор не может расширять обзор арбитра внутри слайса, который этот же арбитр судит). (1) **Бюджет:** `DIFF_CAP` 12000 → 60000 символов, единой константой с override `JUDGE_DIFF_CAP` (сейчас 12000 захардкожены в трёх местах: `budgetDiff`, `slurpDiff`, `slurpExternalDiffs`). Мотив — замер: дифф T010 на 15 файлов давал 800 символов на файл, и verify-судья (codex/gpt-5.6, 77 с, живой ответ) дал block с формулировкой «ключевые production- и test-диффы обрезаны, доказательно проверить нельзя, при REJECT-default это исключает pass» — бюджет резал не хвосты, а суть слайса, то есть пофайловая обрезка T001 честно помечала обрезку, но самой информации судье не хватало. 60K ≈ 15–20K токенов — читается целиком любым из трёх CLI. (2) **Grounding внешнего репо:** файлы внешнего git-корня показываются судье секцией «ВНЕШНИЙ РЕПО», но `checkGrounding` сверяет `filesReviewed` только с `diffFileList(status)` текущего репо — судья, добросовестно назвавший внешний файл, получает безусловный `grounding:phantom-file` (живой прогон 2026-07-29: `~/.claude/bin/elt.js (external repo)` в `filesReviewed` → block). Внешние файлы, реально показанные в промпте, обязаны приниматься; пояснительный суффикс в скобках не делает путь фантомом. Фантом остаётся фантомом: несуществующий путь блокирует по-прежнему. Тесты: `budgetDiff`/`slurpDiff` на 60K-диффе при новом дефолте показывают ключевые файлы целиком (не по 800 символов) и уважают `JUDGE_DIFF_CAP`; `checkGrounding` принимает показанный внешний путь и по-прежнему валит выдуманный. [files: tools/fleet/gate.js, tools/judge-grounding.test.js]

## Фаза F — ускорение (бьёт по замеренным 76%, не по гейту)

Замеры 24.07: impl-сессия = 485 с из ~600 с слайса (76–80%), гейт = 24%. Батч (`elt commit
--task T001,T002,T003`, `-Batch N`) уже есть с 22.07 и даёт ≈1.2× — потолок, потому что
экономит только гейт. Настоящий резерв — разведка имплементатора: 52 turns, до 205K контекста,
codegraph 0.4% вызовов.

- [ ] **T012** `[P]` Карта кода в промпт имплементатора: перед запуском слайса драйвер сам собирает контекст по зоне `[files:]` (codegraph MCP недоступен из `claude -p` без сессии — берём дешёвый эквивалент: `codegraph_files`-подобный листинг зоны + сигнатуры символов через существующий индекс `.codegraph/`, фолбэк — список файлов зоны с размерами) и кладёт секцию «КАРТА ЗОНЫ» в промпт. Цель — убрать 15–25 turns слепого Read на слайс. Замер обязателен: 3 слайса до / 3 после, сравнение turns и времени в run-log. Нет индекса → секция пропускается, поведение старое. [files: tools/elt-loop.ps1, tools/slice-map.js, tools/slice-map.test.js]

- [ ] **T013** `[P]` Дешевле гейт без потери строгости: (1) red-proof стартует ПАРАЛЛЕЛЬНО первому судье, а не после (0 LLM-вызовов, чистый git worktree + тесты — экономит 30–60 с на слайс; результат нужен только к моменту записи proof); (2) авто-батч: `elt slice next --count auto` группирует подряд идущие `[S]`-слайсы одной спеки с непересекающимися `[files:]` (потолок 4, архитектурные/предусловные — не батчить); (3) включить `batch` в живых проектах. Тесты: параллельный red-proof даёт тот же вердикт, что последовательный; auto-группировка не склеивает разные спеки и задачу-предусловие. [files: tools/judge-invoke.js, ~/.claude/bin/elt.js, tools/elt-batch.test.js]

## Фаза E — доказательство

- [ ] **T011** Живой прогон и замер: ≥4 слайса на реальном проекте с включённым контуром (attest + verify + redProof) через драйвер, и ≥3 `[P]`-слайса через fleet с agy-воркерами. Собрать: время гейта до/после, число парковок вместо смертей прогона, сколько раз сработали worker-attestation и failover, содержимое health.jsonl. Отчёт — в `.planning/CHECKPOINT-*.md` + обновление `.planning/STATE.md`. Без этого замера спека не закрывается (метод репо: done только с доказательством). [files: .planning/CHECKPOINT-2026-07-XX-009-live.md, .planning/STATE.md]
