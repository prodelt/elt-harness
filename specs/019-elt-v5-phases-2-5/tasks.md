# 019 — Задачі

> Виконується батчами по 5: один оракул + один суддя + один коміт на батч.
> Порядок батчів обов'язковий: A (шум і батч-режим) → B (зрізання) → C (плагін) → D (реліз).
> Після КОЖНОЇ правки `tools/elt.js` — `node tools/sync-bin.js` у тому ж зрізі, доки
> deploy-замикання живе (знімається аж у T015).

## Батч A — фаза 2: ревʼю по офіційному рецепту

- [ ] **T001** Єдиний модуль `tools/harness-files.js`: одна функція `isHarnessOwned(path)` і один
  список `IGNORE_FOR_REVIEW` (`package-lock.json`, `*.lock`, `.harness/**`, `.git/elt/**`,
  `review-queue.jsonl`, `learnings.jsonl`, `run-log.jsonl`, артефакти прогонів у `.planning/`).
  Сьогодні список дублюється мінімум двічі — `tools/elt-gate-l0.js:199-200` і
  `tools/fleet/gate.js:626-629` — і саме розбіжність дублів дає D9/D15/D19. Обидва місця
  починають кликати модуль. Перевірка: новий `tools/harness-files.test.js` — `package-lock.json`
  і `review-queue.jsonl` не дають `out-of-scope`, вимірювання `diff-size` їх не рахує;
  `node --test tools/elt-gate-l0.test.js` зелений.

- [ ] **T002** Батч-режим перестає бути непрохідним: D13 — зона читає ВСІ входження `[files:]`
  (`.match()` без прапорця `g` бачить лише перше, тому задачі 2..N батча гарантовано
  `out-of-scope`); D14 — поріг `diff-size` масштабується на число задач у батчі
  (константа 400 проти суми по N задачах). Перевірка: регреси в `tools/elt-gate-l0.test.js` —
  батч із трьох задач із різними `[files:]` не дає `out-of-scope`, а дифф 900 рядків на 5 задач
  не тригерить `diff-size`, тоді як 900 на одну задачу — тригерить.

- [ ] **T003** Пʼять лінз ревʼю як окремі субагенти за офіційним рецептом плагіна `code-review`:
  відповідність `CLAUDE.md`, очевидні баги, історія `git blame`, застосовні коментарі з
  попередніх ревʼю, відповідність guidance у коментарях коду. Лінзи — файли `agents/review-*.md`
  із frontmatter (`name`/`description`/`model`/`tools`), запускаються паралельно. Перевірка:
  `tools/review-lenses.test.js` — усі пʼять описів валідні, кожна лінза повертає знахідки у
  форматі `{file, line, summary, failure_scenario, confidence}`.

- [ ] **T004** Оцінювач упевненості на `claude-haiku-4-5-20251001` і відсічка 80 за рубрикою
  рецепта (0–74 — nitpick або невпевненість, 75–100 — реальна проблема): знахідка з
  `confidence < 80` не стає блокуванням, а падає в чергу `elt review` як `weak-signal`. Явний
  список типових хибних спрацювань (згенеровані файли, форматування, «додай тест» без причини,
  стиль без правила в `CLAUDE.md`) передається лінзам у промпті. Перевірка:
  `tools/review-confidence.test.js` на стабі — 79 не блокує, 80 блокує; контракт вердикту
  (`pass`/`inconclusive`/`block`) не змінився, `tools/elt-judge-contract.test.js` і
  `tools/judge-wiring.test.js` зелені.

- [ ] **T005** Замір критерію фази 2: скрипт `tools/measure-noise.js` рахує сигнал/шум по
  останніх 20 блокуючих вердиктах із `.git/elt/run-log.jsonl` (істинне блокування = після нього
  був фікс у тих самих файлах; хибне = знято без правки). Базлайн 1:7. Перевірка: число з датою
  і командою в `.planning/STATE.md` і в реєстрі; жодного вердикту по `package-lock.json`,
  `approval.json`, `review-queue.jsonl` у вибірці; якщо < 1:1 — записати поіменно, які правила
  ще шумлять, як вхід у наступну ітерацію.

## Батч B — фаза 3: зрізання

- [ ] **T006** Витягнути суддю з `fleet/` і зняти `tools/fleet/**` (2 715 рядків коду + 3 065
  тестів). Спершу перенести несучі частини `fleet/gate.js` (`judgeDiff`, схема вердикту,
  grounding) у `tools/judge-invoke.js`, який сьогодні їх імпортує, — це найризикованіша ланка за
  розвідкою. Потім відрізати посилання в `claude-invoke.js`, `elt-verify-bg.js`, `judge-bench.js`,
  `probe-primitives.js`. Перевірка: `grep -rn "fleet/" tools/*.js` порожній,
  `node --test tools/judge-bench.test.js tools/elt-judge-contract.test.js` зелений,
  `node tools/elt.js status` працює.

- [ ] **T007** Зняти PowerShell-драйвери: `elt-loop.ps1` (440), `elt-drive.ps1` (118),
  `elt-fleet.ps1` (59), `elt-selfheal.ps1` (75), `elt-selfheal-lib.ps1` (35) і
  `tools/approval-guard.js` разом із тестом — guard жив рівно для двох викликів
  (`fleet/fleet.js`, `elt-loop.ps1`), обидва щойно померли. Перевірка: `ls tools/*.ps1` лишає
  тільки `doctor.ps1` і `skill.ps1`; `grep -rn "approval-guard\|elt-loop" tools/ .harness/`
  порожній; `node tools/doctor.js` exit 0.

- [ ] **T008** Зняти другу синхронізацію і codegraph-інтеграцію: `sync-agent-surface.js` (351)
  з тестом (518), `elt-harness-sync-all.js` (129), `elt-harness-propose.js` із judge-bench-гейтом
  (D16 — він і так недосяжний), `codegraph-guard.js` (38) і його виклик у `elt-gate-l0.js`.
  `tools/doctor-core.js` чиститься від fleet- і sync-перевірок. Перевірка: `node tools/doctor.js`
  exit 0 без згадок fleet/sync; `node --test tools/doctor.test.js` зелений.

- [ ] **T009** Фаза 3b — мертві підсистеми за інвентарем: `tools/amos-baseline/`, `rag-ingest.py`,
  `rag_queue*.py`, `research*.js`, `hook-diet*`, `skill-search*`, `agent-library.js`,
  `probe-primitives.js`, `stuck-detector.js`, `harness-watch*` і решта, у кого нема жодного
  виклику з живого шляху. Кожен кандидат перевіряється грепом на посилання ДО видалення, а не
  за назвою. Перевірка: для кожного видаленого — рядок «нуль посилань» у комміт-меседжі;
  `node tools/elt-oracle-runner.js --full` exit 0.

- [ ] **T010** Доказ критерію фази 3: `wc -l` тією ж командою, що дала 31 752, і окремо — обсяг
  того, що піде в плагін. Перевірка: число «свого коду» з датою в `.planning/STATE.md`; повний
  оракул exit 0; у `PLAYBOOK.md` кожен сценарій, що спирався на видалене, або має заміну штатним
  засобом (субагенти, `--worktree`), або явно записаний як знятий — мовчазних втрат нема.

## Батч C — фаза 4: плагін

- [ ] **T011** Каркас плагіна: `.claude-plugin/plugin.json` (name `elt`, version `5.0.0`),
  `.claude-plugin/marketplace.json` із власним маркетплейсом, `bin/` — оракул, L0, ledger,
  doctor. Перевірка: схема обох маніфестів валідна, `node --test bin/*.test.js` зелений.

- [ ] **T012** Поверхня плагіна: команди (`/elt`, `/elt-verify`, `/elt-defects`, `/elt-doctor`),
  скіл-маршрутизатор `skills/elt/SKILL.md`, агенти — пʼять лінз із T003 і оцінювач із T004.
  Перевірка: у чистому проєкті команди видно, `/elt-doctor` відповідає.

- [ ] **T013** `AGENTS.md` генерується з єдиного файлу інструкцій (сьогодні копії розʼїжджаються —
  це корінь того, що `agy` не читає скіли). Перевірка: тест на дрейф — згенерований `AGENTS.md`
  збігається з джерелом байт-у-байт, розбіжність = червоний.

- [ ] **T014** CI на двох платформах: `.github/workflows/test.yml` — `node --test` на
  `windows-latest` і `ubuntu-latest`. Перед цим поіменно закрити Linux-несумісності
  (`claude-invoke.js`, `context7-cli.js`, `.cmd`-обгортки, CRLF-залежні тести). Перевірка:
  обидва джоби зелені.

- [ ] **T015** Жива установка і зняття deploy-замикання: у чистому тимчасовому проєкті плагін
  ставиться і `/elt-doctor` зелений без ручних кроків. ТІЛЬКИ після цього знімаються
  `tools/sync-bin.js` (94+92) і побайтна перевірка в `doctor.test.js`: на `~/.claude/bin`
  сьогодні живуть 12 проєктів, зняти копію раніше = лишити їх без харнеса. Перевірка:
  `~/.claude/bin` більше не входить у жоден шлях коду.

## Батч D — фаза 5: реліз

- [ ] **T016** README з чесними числами «до і після» (доля комітів через харнес 18,6% → 62,0%
  у цьому репо, сигнал/шум 1:7 → замір T005, свій код 31 752 → замір T010, дефекти 14 відкритих
  → залишок) і реєстр дефектів окремим розділом. Перевірка: кожне число в README має команду,
  якою його відтворити.

- [ ] **T017** `LICENSE` (MIT) і `CHANGELOG.md` із записами по фазах 0–5. Перевірка: файли є в
  корені, ліцензія збігається з ліцензіями взятих компонентів.

- [ ] **T018** Реєстр дефектів приводиться в чесний стан: кожен D-номер або закритий із доказом,
  або переведений в issue через `gh` із міткою `harness-defect`. Перевірка: у реєстрі нема рядка
  «не виправлено» без посилання на issue; блокуючих відкритих — нуль.

- [ ] **T019** Механіка самофіксації дефектів (розділ 09 концепції): `.elt/ledger.jsonl` пише
  `weak-signal`, `miss`, `false-positive`, `harness-defect`; накопичилось 5 записів одного
  правила — зведення в issue. Перевірка: `tools/ledger.test.js` — усі чотири типи пишуться,
  поріг 5 спрацьовує один раз, а не щоразу.

- [ ] **T020** Фінальне закриття: повний оракул exit 0, зведений KPI по всіх проєктах тим самим
  скриптом, тег `v5.0.0`. Перевірка: `node tools/elt-oracle-runner.js --full` exit 0; число долі
  комітів через харнес із датою в `.planning/STATE.md`; `PROJECT-HISTORY.md` доповнено підсумком.
