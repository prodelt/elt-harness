# 015 — Задачі

- [X] **T001** Прибрати брудне дерево: фікс path-аліасів (`@/`, `~/`, `#`) у `externalImports` та найменший regression-тест на нього. Перевірка: `node tools/elt-gate-l0.test.js` зелений; `git status` чистий.
- [X] **T002** Глобальний SkillSpector v2.8.2: `resolveBinary()` у `tools/skill-scan.js` шукає global-first (PATH → `~/.local/bin` → vendored venv), плюс рядок у `doctor` з наявністю й версією. Перевірка: тест на порядок резолву; `node tools/doctor.js` друкує версію `2.8.2`.
- [X] **T003** Drift-верифікація 2.1.3 → 2.8.2: підтвердити проти реєстру правил v2.8.2, що hard-block ID (`P5`, `AST8`, `TT3`, `TT5`, `YR1..YR4`) і код-категорії (`Behavioral AST`, `Taint Tracking`, `Supply Chain`, `Data Exfiltration`) існують без перейменувань, а JSON-поля, які читає обгортка, на місці. Перевірка: живий прогін `skill-scan.js` на known-bad фікстурі дає `blocked` (exit 3), на чистій — `pass` (exit 0).
- [X] **T004** Живий скан усіх скілів `~/.claude/skills` через `--no-llm`: зберегти JSON-звіт у `.planning/`, звести pass/review/blocked і топ-категорії. Плюс 1–2 скіли з LLM-стадією через `SKILLSPECTOR_PROVIDER=claude_cli` (без API-ключа) для порівняння precision. Перевірка: звіт існує, числа відтворювані повторним прогоном.
- [X] **T005** Ландшафт інструментів безпеки ІІ-агентів через `gh` CLI (WebSearch заборонений): pre-install сканери, runtime-guardrails, red-teaming, сендбокси. Кожен запис — зірки й дата останнього коміту з `gh`, нічого з пам'яті. Перевірка: звіт у `.planning/`, ≥8 інструментів, у кожного перевірені метадані.
- [X] **T007** Перебудувати дек під нетехнічну аудиторію (керівництво + безпека), 9 слайдів на 5 хв, світла тема, каркас «карта дверей + зум»: 4 схеми загроз (промпт-ін'єкція, чужий скіл, крадіжка секретів, надлишкові права), схема місця SkillSpector у роботі агента, один демо-слайд із пійманим шкідливим скілом, підсумкова карта з 4 шарами захисту. Техдеталі — у додаток після підсумку. Перевірка: артефакт оновлено за тим самим URL, 9 слайдів основного потоку, жодного слайда про власні 86 скілів.
- [X] **T008** Тези доповіді окремим документом (`.planning/TALK-skillspector-TEZY-*.md`): по кожному з 9 слайдів основного потоку — хронометраж, тези для проговорювання, місток до наступного слайда, плюс секція «якщо запитають». Із самого деку прибрати нижні блоки `Сказати:` (джерела й застереження лишити). Перевірка: у HTML немає жодного `<b>Сказати:</b>`, дек без переповнення на 1366×768 і 1920×1080, артефакт оновлено за тим самим URL.
- [X] **T009** HTML-тест по доповіді: 10 питань з одним правильним варіантом, миттєва перевірка з поясненням і посиланням на слайд, підсумковий бал із трьома смугами вердикту, кнопка «пройти ще раз». Одна самодостатня сторінка, світла тема деку. Перевірка: 10 питань рендеряться, кнопка перевірки розблокована лише після всіх відповідей, підрахунок балів і показ пояснень підтверджені живим прогоном у браузері.
- [X] **T010** Миттєвий фідбек у тесті: відповідь замикається одразу після кліку — правильний варіант зеленим, вибір користувача червоним, пояснення розкривається, ліва смуга картки й номер питання фарбуються за результатом. Кнопка внизу лише показує підсумковий бал. Перевірка: живий прогін — після одного кліку картка `locked` + `was-ok`, пояснення видиме, повторний клік не змінює вердикт, підсумок рахує вірно.
- [X] **T006** Дек 8–12 слайдів українською як HTML-артефакт: що таке SkillSpector, дві стадії, установка й налаштування (включно з `claude_cli`), мапінг гейта, живі числа з T004, ландшафт із T005, trust model і межі. Перевірка: артефакт опублікований, читається за 5–10 хв.
