# 017 — Задачі

- [X] **T001** Fleet runtime: `linkNodeModules`/`unlinkNodeModules` у `tools/fleet/worktree.js` (D1 — порожній worktree робив оракул червоним у будь-якому Node-проєкті; D2 — `fs.rmSync` йшов крізь junction і виїдав кореневий `node_modules`) і fast-path на `.exe` у `claudeExe()` (`tools/fleet/providers.js`, D18 — `cmd.exe` конкатенував argv, `--json-schema` приїжджав битим). Перевірка: `node --test tools/fleet/worktree.test.js` 5/5 і `node --test tools/fleet/providers.test.js` 26/26.
- [X] **T002** Attest: звірка `undeclared` проти union заявки (`filesChanged` + `testsAdded`) у `tools/fleet/attest.js` — D3, через асиметрію будь-який зріз із новим тестом був непрохідним у принципі (19 відмов із 20 поспіль). Перевірка: `node --test tools/fleet/attest.test.js` 18/18, серед них новий кейс на `testsAdded`.
- [X] **T003** Оновити модель `agy` з `gemini-3.6-flash-high` на `gemini-3.7-flash-high` у `tools/fleet/router.js` і в двох дефолтах `tools/elt-loop.ps1`, а також у `JUDGE_MODELS` (`tools/elt.js:605` — рядок був пропущений, через що deploy-копія `~/.claude/bin/elt.js` розійшлася з репо і `doctor.test.js` був червоний). Оракул спіймав третє місце прив'язки: `tools/fleet/effort-policy.test.js:107` прибитий до старої назви моделі й падає на новому дефолті — тест і є контракт, тому входить у зріз. Перевірка: `node --test tools/fleet/router.test.js` 14/14 і `node --test tools/fleet/effort-policy.test.js` 7/7.
- [X] **T004** L0 не має судити документний дифф кодовими правилами: `externalImports` (`tools/elt-gate-l0.js`) тепер читає заголовок ханка (`+++ b/path`) і рахує імпорти ЛИШЕ в кодових файлах. Живий блок 2026-08-22: документний коміт архіву (11 `.md`, 2955 рядків) зупинено тригером `external-import-no-ctx7` на слові `vitest` із ЛІСТИНГА КОДУ всередині field report — а це єдиний тригер, що сам виносить `block`, тож L0 закривав документні двері (016 T010), перед якими стоїть. Перевірка: `node --test tools/elt-gate-l0.test.js` — новий кейс `testDocDiffIsNotJudgedAsCode` (лістинг у `.md` не імпорт; той самий лістинг у `.js` лишається видимим).
- [X] **T005** `changedFiles()` (`tools/elt.js`) має отримувати сирі шляхи: git за замовчуванням віддає шлях із не-ASCII байтами в C-лапках (`"\320\234..."`), і документні двері бачили імʼя, яке не закінчується на `.md` і не починається з `.planning/` — файл `Методология Agent Harness.md` було оголошено кодом і двері зачинилися. Фікс — `-c core.quotepath=false` в обох викликах. Живий відказ 2026-08-22 у тексті помилки `elt commit`. Перевірка: `node --test tools/elt-docs-commit.test.js` 3/3, новий кейс «документ з не-ASCII іменем».
- [X] **T006** Регрес на D1/D2 у `tools/fleet/worktree.test.js`: пʼять наявних тестів проходять байт-у-байт навіть якщо `linkNodeModules`/`unlinkNodeModules` видалити зовсім — у фікстурі немає `node_modules`, обидві функції йдуть у ранній вихід. Знайшов ФОНОВИЙ СУДДЯ на комміті `76c40bb`, і він має рацію: заявлена перевірка «worktree 5/5» нічого про фікс не доводила. Новий кейс кладе `node_modules/.bin/marker.txt` у корінь, перевіряє лінк після `create()` і цілість кореня після `remove()`. Уточнення до реєстру дефектів: крізь junction іде НЕ `fs.rmSync` (Node 24 його лише відвʼязує), а сам `git worktree remove --force` — відтворено 2026-08-22 на чистій фікстурі. Перевірка: `node --test tools/fleet/worktree.test.js` 6/6.
