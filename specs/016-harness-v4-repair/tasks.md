# 016 — Задачі

## Фаза A — правда сигналу (без неї решта не має сенсу)

- [X] **T001** Фон бере команду оракула з конфігу проєкту, а не з дефолту: `spawnBackgroundVerify` (`tools/elt-verify-bg.js:94`) читає `harnessField(cwd, 'oracle')` і кладе значення в `bgChildEnv`; дефолт `oracleCmd` у `runBackgroundVerify` (рядок 186) прибрати або звести до явної помилки, щоб мовчазний фолбек на чужий шлях став неможливим. Перевірка: `node tools/elt-oracle-runner.js` зелений; у фоновому лозі домашнього репо видно саме команду з `harness.json`, а не захардкожену.
- [X] **T002** Regression-тест, який ловив би цей дефект: прогін `runBackgroundVerify` **без** підстановки `ELT_VERIFY_BG_ORACLE_CMD` у фікстурному репозиторії з власним полем `oracle` — тест падає на поточному коді й зеленіє після T001. Перевірка: `git stash` фікса T001 → тест червоний; з фіксом — зелений.
- [X] **T003** Шар `mutate` (`tools/elt-verify-bg.js:213`) бере ту саму команду з конфігу замість захардкоженого `'node tools/elt-oracle-runner.js'`. Перевірка: тест на те, що `runTests` отримує команду проєкту; існуючі тести мутатора зелені.
- [X] **T004** Прибрати флоки з фонової смуги: знизити паралелізм саме у фоновому прогоні (`jobs=8` там не потрібен — час безкоштовний) і дати запас за таймаутами трьом найповільнішим тестам (`fleet.test.js` 150 c, `elt-config.test.js` 117 c, `harness-watch.test.js` 101 c). Перевірка: три фонові прогони поспіль на документних слайсах дають `background-verify-pass`; жодного `сьют: exit 1` без реальної причини в лозі.
- [X] **T005** [крос-проєктна поверхня] Живий прогін фону в чужому проєкті — Portfolio (Next.js, `oracle: "npx tsc --noEmit && npm run lint"`), той самий, де дефект зафіксовано. Перевірка: `background-verify-pass` із тривалістю шару `suite`, співмірною з ручним прогоном оракула проєкту, а не 0,13 c. Це і є закриття парковки T019/T020 зі спеки 014.

## Фаза B — замкнути еволюцію

- [ ] **T006** Зберегти облікові дані для завдання `ELT-retro-label` (зараз `LastTaskResult=2147946720` = `0x80070520`, ERROR_NO_SUCH_LOGON_SESSION). Перевірка: примусовий запуск через планувальник дає `LastTaskResult=0`, у `.harness/learnings.jsonl` з'являється новий запис із датою прогону.
- [X] **T007** Замкнути `benchAdded`: розмічені кейси реально поповнюють `judge-bench` (зараз 196 розмічених, `benchAdded: 0`, файли bench від 22.07). Знайти обрив між ретро-розміткою і записом у bench. Перевірка: повторний прогін ретро-розмітки збільшує `benchTotal`; у `judge-bench/` з'являються файли з новою датою.

## Фаза C — розкатка й облік

- [X] **T008** `elt harness sync-all`: пройти реєстр проєктів, показати дифф кожного `.harness/harness.json` проти поточної схеми (`verify`, `background.layers`, `oracleSelect`, мертве `judge.verify`), застосувати з підтвердженням. Скрипт матриці з аудиту — готові 80%. Перевірка: `--dry-run` друкує дифф для 14 проєктів; застосування до одного проєкту додає поля v4 і прибирає `judge.verify`.
- [X] **T009** `elt commit` або пише run-log, або голосно відмовляється. Причина: AWE4 має `.fleet-wt/T105`, `T106` і коміти `chore: elt slice`, але `.git/elt/` немає взагалі (перевірено і основний gitdir, і gitdir'и приєднаних worktree) — робота велася, харнес про неї не знає. Перевірка: у фікстурному репо з недоступним для запису шляхом run-log `elt commit` завершується ненульовим кодом із названою причиною, а не мовчки.
- [X] **T010** Закрити зазор, знайдений при закритті аудиту: `elt commit` вимагає `--task Txxx` навіть для суто документного коміту, де задачі в плані немає й бути не повинно (довелося комітити вручну). Дати режим коміту без задачі для `docs:`/`chore:` без коду. Перевірка: коміт лише документних файлів проходить через `elt commit` без `--task`; коміт із кодом без `--task` так само відхиляється.

## Фаза D — швидкість (тільки після фази A)

- [X] **T011** Винести суддю з критичного шляху повністю: синхронно лишається лише L0 (мілісекунди), суддя цілком їде у фон із наявною чергою `bg-red`. Підстава: `l0-clean` = 19 проти 299 викликів судді, p50 = 61 c (у tg_bot_reclamaties 214 c) — саме суддя, не оракул, тримає довжину циклу. Перевірка: п'ять слайсів поспіль із синхронною частиною < 10 c; жоден вердикт не втрачено — усі присутні у фоновій черзі; порівняння p50 циклу проти 16 хв базлайну.

## Фаза E — за рішенням користувача, не обов'язкові для закриття

- [ ] **T012** CodeGraph: adoption 0 викликів при живому індексі (322 файли, 4 642 вузли). Або вбудувати виклик у прелюдію слайса (`elt brief` — природне місце), або визнати мертвим і зняти рядок із `doctor`. Перевірка: за рішенням — або `doctor` показує ненульовий adoption після трьох слайсів, або рядка немає.
- [ ] **T013** `stryker-mutator/stryker-js` (3003★) у фонову смугу замість саморобного шару `mutate`, який майже завжди відповідає «немає змінених прод-файлів». Тільки у фон, ніколи в синхронний гейт. Перевірка: один прогін дає реальний mutation score на цьому репо; синхронний цикл не подовшав.
- [ ] **T014** Прочитати failure-mining у `neosigmaai/auto-harness` (530★) рівно заради проблеми з T007 і записати, що з нього застосовне. Перевірка: нотатка в `.planning/` із конкретними висновками, не переказ README.

## Поза планом — рішення користувача зі спеки 014

T004 спеки 014 (замір фази A проти базлайну) і T021 (підсумковий замір) лишаються за користувачем.
`doctor` FAIL «Codex: `danger-full-access` + `approval=never` як дефолт» — окреме рішення, не слайс.
