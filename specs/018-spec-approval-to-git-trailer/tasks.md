# 018 — Задачі

- [ ] **T001** Нормалізація `tasks.md` перед хешуванням: функція `normalizeTasks(text)` у `tools/elt.js` зводить стан галочок до одного вигляду (`- [X]`/`- [x]` → `- [ ]`) і лишає решту тексту байт-у-байт; `readSpecHashes()` рахує `tasksHash` уже від неї. Це вбиває D7 і D11 у корені, ще до зняття файлу. Перевірка: новий кейс у `tools/elt-spec-approval.test.js` — той самий план із `[ ]` і з `[X]` дає ОДИН хеш, а зміна тексту задачі — різні; `node --test tools/elt-spec-approval.test.js` зелений.

- [ ] **T002** Читання підпису з історії: `readApprovalTrailer(specDir, cwd)` у `tools/elt.js` шукає в `git log --format=%H%x00%B` коміт із трейлерами `Spec-Approved: <specDir>`, `Spec-Hash:`, `Tasks-Hash:` і повертає перший, чиї хеші збігаються з поточними. Джерело — історія, тому відповідь однакова в основному дереві й у worktree (D4). Перевірка: новий тест `tools/elt-spec-trailer.test.js` на темп-репо — трейлер знайдено в основному дереві та в `git worktree add` на тому ж коміті.

- [ ] **T003** `elt spec approve` пише трейлер: якщо спека має незакомічені зміни — комітить її з трейлерами; якщо вже в історії — робить порожній коміт `chore: approve spec NNN` з тими ж трейлерами. `approval.json` більше не створюється. Перевірка: `tools/elt-spec-approve.test.js` — після `approve` файла немає, `git log -1 --format=%B` містить три трейлери, повторний `approve` без змін не створює другий коміт.

- [ ] **T004** Гейт `specApprovalGateFor()` читає трейлер, а `approval.json` приймає лише як міграційну пільгу з гучним попередженням у stderr (`застарілий шлях, перепідпишіть: elt spec approve`). Обидва місця виклику (`slice next`, `commit`) не змінюють сигнатуру. Перевірка: `node --test tools/elt-approval-gate.test.js` — трейлер пускає, відсутність підпису блокує, старий `approval.json` пускає з попередженням.

- [ ] **T005** Прибрати `tools/approval-guard.js` разом із викликами: `tools/fleet/fleet.js` (імпорт і виклик перед прогоном) і `tools/elt-loop.ps1` (pre-run блок). Гейт лишається один — у `elt commit`; передчасна перевірка робочого дерева і була механізмом D4. Перевірка: `grep -rn "approval-guard" tools/` порожній; `node --test tools/fleet/fleet.test.js` зелений; `tools/approval-guard.test.js` видалений разом із модулем.

- [ ] **T006** Живий прогін критерію фази 1 на спеці з 9 задач: узяти реальну спеку з ≥9 відкритими задачами (або завести фікстурну в темп-репо) і закрити всі дев'ять поспіль. Порахувати число переутверджень. Перевірка: рівно ОДНЕ затвердження на всі 9 закриттів, у `run-log` дев'ять записів коміта, жодного `спека не утверждена`; результат вписати в `.planning/HARNESS-DEFECTS-REGISTRY-2026-08-21.md` як закриття D4/D7/D11/D15.

- [ ] **T007** Синхронізувати deploy-копію і зачистити згадки: `node tools/sync-bin.js`, прибрати рядок про `approval.json` з `elt --help`, оновити `CLAUDE.md` і `PLAYBOOK.md` на новий шлях, зняти кейс `spec-approval-*` з `judge-bench/cases.js`, якщо він спирався на файл. Перевірка: `node tools/elt-oracle-runner.js --full` exit 0 і `cmp tools/elt.js "$env:USERPROFILE\.claude\bin\elt.js"` без різниці.

- [ ] **T008** Зняти міграційну пільгу: `approval.json` більше не приймається, а якщо знайдений — гучна підказка перепідписати. Пройтися реєстром проєктів (`elt harness sync-all`) і перевірити, що жоден чужий проєкт не лишився без трейлера. Перевірка: тест на те, що самотній `approval.json` тепер блокує; список проєктів із результатом у `.planning/STATE.md`.
