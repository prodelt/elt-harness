# CHECKPOINT 2026-07-31 (2) — 011/T016 + 010/T004 ЗАКРЫТЫ, resume хвост 010 (T006/T007/T008)

Предыдущий: `.planning/CHECKPOINT-2026-07-31-011-T001-closed-redproof-hang.md`.

## СДЕЛАНО

### 011/T016 закрыт — коммит `cf419ea`
Блокер хвоста 010 расшит. Два фикса в `tools/red-proof.js`:
- **Потолок на файл** — `harness.json.redProofTimeoutMs` (дефолт 5 мин), превышение → явный
  `skipped: test-cmd-timeout:<файл>` с хвостом вывода. Раньше `spawnSync` был без `timeout` —
  зависание молчаливое и бесконечное (живьём 25 минут).
- **Прогон по ОДНОМУ файлу** — `node -- a.js b.js` запускал бы только `a.js` (остальное ушло бы
  в argv скрипта), т.е. молча доказывал бы лишь первый тест. Первый упавший = слайс доказан.
- `.harness/harness.json` → `"testCmd": "node"` (дефолт `node --test` для этого репо ложен:
  тесты тут самозапускающиеся `main()`-скрипты) + `"redProofTimeoutMs": 120000`.

Тесты `tools/red-proof.test.js` — **8/8** (3 новых: подхват `testCmd` из конфига через маркер
`NODE_TEST_CONTEXT`, таймаут, прогон всех файлов а не первого). Оракул 61/61 за 213 c, судья
`pass` за 15.9 c.

Живой пруф механизма: red-proof на своём же диффе вернул `skipped: test-cmd-timeout:tools/red-proof.test.js`
за 301 c с дефолтным потолком — старый код на этом же тесте висел бы бесконечно.

### 010/T004 закрыт — коммит `0377f07`
`checkJudgeBridge` в `doctor-core.js` + тест на 5 ветвей. Оракул 61/61 за 196 c, судья `pass`
за 17.8 c, **red-proof `red: fails-on-base`** (`checkJudgeBridge is not a function` на baseHead) —
т.е. фикс T016 реально разблокировал этот слайс: вчера он вешал гейт на 25 минут.

`sync-bin` прогнан (8 файлов → `~/.claude/bin/judge`). Убит осиротевший `node --test` от
вчерашнего зависания (жил с 12:07) и два мусорных `red-proof-*` worktree.

## ЗАМЕР ГЕЙТА — тормоза переехали в ОРАКУЛ

Два живых слайса подряд:

| слой | время |
|---|---|
| **оракул (61 файл, jobs=8)** | **196–213 c ≈ 90% слайса** |
| судья (agy, один, после снятия verify) | 15.9–17.8 c |
| red-proof | до 120 c потолка |

Хвост оракула держат три файла со spawn'ами внутри: `fleet.test.js` 135–146 c,
`harness-watch.test.js` 106–116 c, `doctor.test.js` 70–79 c. Гоняется весь набор на каждый слайс
независимо от зоны.

Задача под это **уже была в 011 — T006 фазы D** (`oracleSelect: "impact" | "all"`). Дубль не
заводился; в T006 дописан этот замер. По времени фаза D бьёт фазу B (L0 экономит 16 c судьи).

## СОСТОЯНИЕ ДЕРЕВА

Закоммичено: `cf419ea` (011/T016), `0377f07` (010/T004).

**Грязно:** `specs/011-elt-v3-gate/tasks.md` — дописан замер в T006. Из-за этого
**спека 011 = `stale`**: нужен `elt spec approve --spec specs/011-elt-v3-gate` ПОСЛЕ явного
«утверждаю» от юзера. Пока stale — слайсы 011 не коммитятся (exit 4); хвост 010 не затронут.

`stash@{0}` = `010-T006-T007-T008`: `project-bootstrap.js`/`.test.js`,
`project-docs-core.js`/`.test.js`. `stash@{1}` = `batch2-T005-T009-T010-T011` (цел, из 29.07).

## ДАЛЬШЕ

1. **Хвост 010 — T006, T007, T008 по одному слайсу.** `git stash pop`, затем резать по зонам:
   все три трогают `tools/project-bootstrap.js` + `.test.js` (T008 ещё `project-docs-core.js`) —
   разделять придётся руками по диффу, стэшить чужие куски перед каждым гейтом.
   Зоны из `specs/010-judge-delivery/tasks.md`: T006 контракт `judgeBridge` в
   `project-bootstrap verify`; T007 `checkOracleVerifierContract` исполняемый при `--deep`;
   T008 `unknownSections` наружу из красного.
2. **Отдельным слайсом: вернуть `checkLoopJudgePath`** (код — в предыдущем чекпоинте) вместе с
   ТЕСТОМ и фиксом драйвера: `tools/elt-loop.ps1:397` пишет вердикт через `judge-proof write`
   при `judge.attest: true` → автономный драйвер сломан прямо сейчас. Задачи под это ещё нет.
3. **011/T002+** по `specs/011-elt-v3-gate/tasks.md` — после approve.

## ГОЧТЫ (к прежним)

- **Гейт-цепочку гнать ОДНИМ фоновым вызовом** `oracle && judge run && commit --skip-oracle`
  и в дерево между шагами не писать (`stale-tree`). Один слайс ≈ 4–6 минут — это нормально,
  90% из них оракул, не зависание. Диагностика «стоит или идёт» — по дереву процессов
  (`Get-CimInstance Win32_Process`), а не по пустому логу.
- **`red-proof.test.js` теперь всегда даёт `skipped:test-cmd-timeout`** на слайсах, которые его
  трогают: он содержит намеренно зависающий кейс, и на baseHead без фикса тот висит. Это
  ожидаемо и не блокирует (блокирует только `green`), но стоит 120 c.
- Правка `tasks.md` утверждённой спеки → `stale`. Approve — акт юзера, не агента.
