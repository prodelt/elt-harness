# CHECKPOINT 2026-07-22 — 008 усиление судьи: T001+T002 закрыты, resume T003

Ветка `feature/judge-bench-parallel-oracle`. Коммиты сессии:
`7f8183b` (007 целиком), `93b0070` (спека 008), `827060b` (T001 grounding), `135a83b` (T002 двойной судья).

## Контекст задачи

Пользователь: судья `agy/gemini-3.6-flash-high` быстрый (42с), но из-за этого некачественный —
галлюцинирует. Нужно «подтверждение реальное доказанное, что всё работает как задумано».
Решения зафиксированы (грилл 2026-07-22): три слоя поверх судьи — grounding-чек (механика),
двойной судья (agy → подтверждает codex `gpt-5.6-sol`), red-proof (новые тесты обязаны падать
на коде ДО слайса). Второй судья = codex (не ест Claude-лимит, ошибается в безопасную сторону).
Порядок: 008 → остаток 006 драйвером под усиленным гейтом.

## Сделано

**007 harness-speed — закрыта целиком** (T001-T003 одним батчем, `7f8183b`):
judge-bench 9→14 кейсов (+удалённый тест, захардкоженный секрет, off-by-one в деньгах,
сломанная сигнатура, чистый docs-кейс); `tools/elt-gate-e2e.test.js` — e2e реального гейта
(настоящий `elt.js` + git, красный путь тоже); `tools/judge-bench-compare.js`. Оракул 48/48.

**008 T001 grounding-чек** (`827060b`, оракул 49/49, судья sonnet pass со 2-й попытки):
- `checkGrounding` в `tools/fleet/gate.js`: `phantom-file` / `unreviewed-file` / `no-reasons`,
  любой отказ форсирует `block` НЕЗАВИСИМО от вердикта модели.
- Секция «ФАЙЛЫ ДИФФА» в промпте берётся из `git status` (не из диффа — тот режется по cap).
- `VERDICT_SCHEMA` и JSON-хвост для codex/agy расширены полем `filesReviewed`.
- **Две правки против первой реализации драйвера** (обе — результат живого прогона):
  1. `phantom-file` = путь, которого НЕ существует на диске. Первый вариант («не в диффе =
     фантом») дал ЛОЖНЫЙ block на честном `pass`: судья перечислил `spec.md`/`tasks.md`,
     которые ему показывают как рубрику. Регресс-тест на этот кейс в `judge-grounding.test.js`.
  2. `no-reasons` — БЕЗУСЛОВЕН, до проверки `filesReviewed`. Судья sonnet заблокировал первую
     реализацию по делу: чек был спрятан за `filesReviewed === null`, то есть провайдер без
     structured output (codex/agy!) снова мог провести `pass` с пустым reasons — мотивирующий
     баг спеки (`7f8183b`) остался бы живым.
- Побочно: 7 стабов в `fleet.test.js` + 1 в `doctor.test.js` печатали `{"verdict":"pass"}`
  без reasons → приведены к реальному контракту судьи (ассерты НЕ ослаблены).

**008 T002 двойной судья** (`135a83b`, оракул 49/49, судья pass) — закрыт драйвером.

## Открытые дефекты (найдены живьём, чинить в T004)

1. **`elt-loop.ps1:256` пишет `--reasons-json "[]"` жёстко** — обоснования судьи НЕ попадают
   в judge-proof. Пруф, ради которого весь протокол, содержательно пуст. Это часть T004
   (проводка `judges[]`/`grounding`/`redProof` из `runJudge` в proof).
2. **Approval-гейт глушит автономный драйвер после КАЖДОГО слайса.** `elt commit` ставит
   `[X]` → `tasksHash` меняется → `status: stale` → следующий `elt slice next` = exit 4,
   драйвер останавливается («слайсов закрыто 1» вместо 3). Сейчас обходится ручным
   `elt spec approve` между слайсами. Нужен self-renew в CLI: diff `tasks.md` ровно
   `[ ]`→`[X]` (та же логика, что SKILL.md разрешает агенту) → тихо перевыпустить подпись.
   **Слайса под это в плане НЕТ** — спека 008 утверждена без него; добавление = вопрос
   пользователю (T006 в 008 или баг-слайс в 006).

## Дальше

- **T003 red-proof** — запущен драйвером в момент написания чекпоинта (`bwy93ocua`).
- **T004** проводка контракта + оба дефекта выше.
- **T005** перемер judge-bench (14 кейсов) на усиленном контуре + живой прогон + включение
  `judge.verify` в `.harness/harness.json` репо. Делать руками (нужны цифры и сравнение).
- Потом: **006** T009-T018 (10 слайсов) драйвером под усиленным гейтом; **005** T021
  (Fleet ledger). 005 T022/T023 — внешние блокеры (Ametryn A/B + pilot approval).

## Ритуал между слайсами (пока дефект №2 жив)

```powershell
node "$env:USERPROFILE\.claude\bin\elt.js" spec approve --spec specs/008-judge-strength
powershell -File tools\elt-loop.ps1 -Project . -SpecDir specs/008-judge-strength -Slices 1 -Batch 1 -JudgeProvider claude -JudgeModel sonnet
```
