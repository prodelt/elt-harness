# CHECKPOINT 2026-07-06 — v2 судовий бот + аудит системи

## Resume Pointer
Юзер попросив (Fable 5, поки доступ є): глибокий аудит harness-системи + elt-loop + стилю роботи,
глибоке вивчення `sudovi-master`, і **безкоштовний шлях до результату платного YouScore** — новий,
швидкий, оптимізований бот, можливо іншою мовою/технологією. Головне — ПЛАН + підготовка elt-loop
+ окремий roadmap.

## Зроблено
- Прочитано: PLAYBOOK, STATE, elt-code/elt-loop SKILL.md, sudovi-master (CLAUDE/README/docs/клієнти).
- **Перевірено живьём** офіційні безкоштовні джерела через law-MCP: ЄДРСР fetch-by-ID працює
  (обхід капчі), data.gov.ua має дневний дамп ЄДРСР (ДСА, ZIP), капча на live-скрейпінгу реальна.
- **Стратегія:** будувати свій індекс за кодом сторони з офіційного дампу (те, що продає
  OpenDataBot), замість злитих ключів (нелегально, штраф 100k грн) чи повільного скрейпінгу.
- **Скаффолд бота** `D:/Ametrin projects/sudovi-fast/`: constitution (5 інваріантів) + spec
  (архітектура+докази) + tasks.md (**19 слайсів, 6 фаз**, оракул на кожному) + STATE (kill-switch) +
  ROADMAP + README. Готовий під `/elt-loop`.
- **Аналіз системи** → `.planning/SYSTEM-ANALYSIS-AND-COURTBOT-PLAN-2026-07-06.md`: вердикт
  harness = зрілий, розрив у ВИКОРИСТАННІ не фічах; головний урок — elt-loop сильний рівно як
  його оракул → v2 бот будувати test-first офлайн; НЕ додавати машинерію (система завершена).

## Наступний крок
```
cd "D:/Ametrin projects/sudovi-fast" && git init && git add -A && git commit -m "chore: scaffold"
/elt-loop     # T001 = верифікація схеми дампу ЄДРСР
```

## Відкрито (за юзером)
1. Мова стеку: Go (реком.) vs Python-async — слайс T002.
2. Пріоритет часу: v2 бот як live-fire #2 vs демо-підготовка 07.07.
3. Пороги паритету/швидкості (T014/T015).
4. Чи коммітити `sudovi-fast` як окремий репо зараз.
