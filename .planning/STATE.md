# STATE — elt-code (петля) + elt-work (офис)

> Живой хребет работы (loop-engineering STATE.md паттерн). Память В ПРОЕКТЕ, не в корне ПК.
> План: `~/.claude/plans/imperative-churning-oasis.md`. Дедлайн демо: **вторник 07.07.2026** (проектор, машина юзера).

## Метод (dogfood — по требованию юзера)
Строим систему самой системой: constitution → spec.md → tasks.md → loop (механический оракул) → checkpoint.
Оракул для этой мета-работы: `node --check tools/*.js`, `node tools/doctor.js`; для AWE3 — `just test` / `just fitness`.
Красные линии: собирать из готового (новый скилл <100 строк), оракул=тесты не судья, «done» только с выводом команды.

## Задачи (P1–P8)
| # | Задача | Статус |
|---|---|---|
| P1+P2 | elt-loop: автономная спек-драйвен петля | ✓ ЖИВОЙ ПРОГОН: US1 (T027-T035) ПОЛНОСТЬЮ ЗАКРЫТА, 6 слайсов подряд разными сессиями, `just test`+`just e2e` зелёные, коммиты через husky-гейт |
| P3 | память/чекпоинты В проект (`.planning/STATE.md`) | ✓ ЖИВОЙ (этот файл) |
| P4 | project-bootstrap reconcile «мусорного» репо | ✓ v1.3.0 (память-в-проект + git-гигиена); AWE3 в порядке |
| P5 | elt-onboard: грилл + анализ проекта → self-config v1 | СКИЛЛ ✓; live-test грилла = нужен юзер |
| P6 | elt-work: офиц. Anthropic office-скилы + роутер + чеклист | ✓ ДОКАЗАН: report.xlsx+docx на диске |
| P7 | loop-engineering черри-пик (STATE spine + loop-audit score) | STATE-хребет ✓; **репетиция демо ПРОШЛА** (2026-07-01, agent-browser) — Scenario 1+2 работают безупречно; loop-audit всё ещё заблокирован auto-mode (юзер) |
| P8 | обрезать CLAUDE.md → actionable-ядро | ✓ (CLAUDE.md ~46 строк; живое состояние — не хардкод) |

## Демо-репо
`C:\Ametrin projects\Ametrin web ecosystem 3` (AWE3) — Rust+Node, spec-kit стоит, `justfile` оракул, husky-зубы (live-fire 2026-06-19), `specs/001-service-aggregator-platform/tasks.md` = источник слайсов.

## Текущий фокус
**006 ELT Front Gate (`specs/006-elt-front-gate`) — T001-T008+T019 ЗАКРЫТЫ.** Судья-межрепо-слепота
починена (`tools/fleet/gate.js` видит diff файлов `[files:]` вне cwd-репо, 006 T007-блокер снят).
T007 (grill-me v2) и T008 (elt SKILL.md Режим 0 v2) закрыты sonnet-судьёй. Resume:
`.planning/CHECKPOINT-2026-07-22-006-T007-T008-closed-resume-T009.md`. Дальше Фаза C: T009
(`elt loop [N]` подкоманда) → T010-T018 тем же ритмом (implement → оракул → sonnet-судья → commit).

### (архив фокуса) ELT Control Plane Convergence
`specs/005-elt-control-plane-convergence` — T001-T020 закрыты, T021 (Fleet ledger) код-фиче доступна, T022/T023 внешние блокеры (paid API A/B + pilot approval). Resume при возврате: `.planning/CHECKPOINT-2026-07-16-T019-T020-closed-resume-T021.md`.

### (архив фокуса) AWE3-демо
US1 (MVP, T020-T035) и **US2 (Phase 4, T036-T045) ОБЕ ЗАКРЫТЫ** — чекпоинт tasks.md
«User Stories 1 AND 2 both work independently» достигнут. Следующий фронтир — **Phase 5, US3**
(«workflow survives one module being down», T046-T052): часть уже может быть закрыта
ahead-of-time (module-sdk circuit-breaker уже в `dispatch()`-стеке с T031; `view_for` уже
не пропагирует `?` между items) — нужна проверка тестами (T046-T048), не обязательно новый
код. T050 (join_all-конкурентность) и T051 (frontend `apps/web/src/unavailable/`) — реальные
дизайн-решения, не однозначны с ходу. Демо-дедлайн 07.07.2026 (6 дней) — P5 live-грилл и P7
репетиция всё ещё не закрыты.

## Разблокировано ✓
`just 1.55.1` установлен. Docker поднят. AWE3 baseline закоммичен (`f39fae3`), дерево чистое, весь husky-гейт зелёный (fmt→boundaries→deny→check→clippy→cargo test --workspace→pnpm).

## Журнал
- 2026-07-01: grill (7 вопросов) → план утверждён → dogfood-харнесс поднят (STATE.md). Офисные скилы подтверждены (anthropics/skills: docx/xlsx/pptx/pdf/doc-coauthoring/internal-comms).
- 2026-07-01: P1 — `elt-loop` SKILL.md собран (тонкий, из auto-ship, оракул=тесты, судья=advisory, close→STATE.md). `elt-code` провёден на `/elt-loop` для автономного маршрута.
- 2026-07-01: Дожфуд `elt-loop` (предусловия) на AWE3 поймал 3 env-блокера ДО тяжёлого Rust-слайса. `.gitignore` починен (+/target). AWE3 на 28/56 задач, следующая = T027.
- 2026-07-01: `just 1.55.1` установлен ✓. Пока Docker down — параллельно закрыт **P6 MVP** (6 office-скилов + роутер `elt-work` + шортлист) и **P5** (`elt-onboard` собран) и **P8** (CLAUDE.md обрезан).
- 2026-07-01: Docker поднят → AWE3 baseline-коммит `f39fae3` на ветке `feature/us1-slice2-aggregation`. Husky-гейт СРАБОТАЛ (зубы!): первый коммит блокнут на `cargo fmt --check` → `cargo fmt` → повторный коммит прошёл весь гейт (fmt→boundaries→deny→check→clippy→**cargo test --workspace**→pnpm) зелёным. Дерево чистое.
- 2026-07-01: Прочитаны движущие части слайса (module-sdk API, contracts, repositories, router). **Найдена design-развилка**: capabilities не в схеме `ServiceModule`. Юзер выбрал **A** (fetch_manifest в момент view).
- 2026-07-01: **P1 LIVE-FIRE ✓** — слайс US1-2 реализован по A: `AggregationService` + handler + роут `GET /api/v1/workflow/view` + контракт-тест с in-test mock-модулем. Оракул `cargo test -p gateway` = **7 passed / 0 failed**. T027/T031/T032 `[X]`. Closeout-коммит `99625c7` через полный husky-гейт (fmt→clippy→cargo test --workspace→pnpm) зелёным. **Петля elt-code доказана end-to-end.**
- 2026-07-01: **P6 ДОКАЗАН** — office-libs (`openpyxl`,`python-docx`) в системный Python311; сгенерены `.planning/office-demo/report.xlsx`+`report.docx` с контентом. ⚠ DEMO-ГОТЧА: ambient `python`=Hermes-venv без libs → office на демо через `py -3` (см. `elt-work-office-research.md`).
- 2026-07-01: **P4 ✓** — `project-bootstrap` v1.3.0: шаг 5 память→`.planning/STATE.md` В ПРОЕКТЕ (не корень ПК); шаг 3 git-гигиена (артефакты в .gitignore + baseline на ветке). **P8 ✓** ранее. loop-audit (P7) заблокирован auto-mode классификатором — юзер запустит сам.
- 2026-07-01: ИТОГ сессии — 7/8 пунктов сделаны (P1,P3,P4,P5-скилл,P6,P8 + P7 частично). Осталось: live-test грилла P5 (нужен юзер) + репетиция демо (P7). Точка чекпоинта.
- 2026-07-01: **Коммит-политика** (решение юзера) зашита в скилы: `elt-loop`/elt-code = АВТО-коммит-на-зелёном (автономность); `elt-work`/нетехнари = ВСЕГДА ручное подтверждение, без авто-коммита/перезаписи.
- 2026-07-01: **Ещё слайс петли на AWE3** — `/elt-code продолжить роадмап` → `/elt-loop`, SLICE=T028 (integration-тест quickstart Scenario 1: select→reload→ещё выбран+рендерится). Новые файлы `crates/gateway/tests/integration.rs` + `tests/integration/composed_workflow.rs` (цепочка registry→workflow PUT→workflow/view через свежие Router-инстансы). Оракул `just test` зелёный с первого прогона (fmt→clippy→cargo test --workspace: 10 passed→pnpm turbo test). Коммит `7cf229f` на ветке `feature/us1-slice2-aggregation` через husky-гейт. T028 `[X]` в tasks.md.
- 2026-07-01: **«продолжай автоматически»** → петля продолжила без остановки. SLICE=T033 (catalog UI): `apps/web/src/catalog/Catalog.tsx` (fetch registry+workflow, add-to-workflow через PUT) + `apps/web/src/identity.ts` (ponytail: employee-id = localStorage-заглушка, auth вне scope всех задач tasks.md) + `App.tsx` подключил `<Catalog/>`. Оракул `just test` зелёный (build+lint+vitest+cargo). Коммит `afdfba6` через husky-гейт. T033 `[X]`. **Остановлено на пороге автокомпакта** (chekpoint `.planning/CHECKPOINT-2026-07-01-elt-loop-T028-slice.md`, обновлён под T033). Следующий слайс — T034 (workflow view UI, `apps/web/src/workflow/`), затем T035 (Playwright e2e US1), затем Phase 4 US2 (T036+).
- 2026-07-01: Новая сессия, `/elt-code продолжай` → `/elt-loop` SLICE=T034 (workflow view UI): `apps/web/src/workflow/WorkflowView.tsx` (fetch `GET /api/v1/workflow/view`, рендер каждого item независимо — `ok`→`data` как JSON, `unavailable`/`forbidden`→`reason`, Principle III: один фейл не блокирует остальные) + `App.tsx` подключил `<WorkflowView/>`. Оракул `just test` зелёный (7+1 cargo, vitest), доп. `pnpm build`+`pnpm lint` чисто. Коммит `fe027d4` через полный husky-гейт (fmt→boundaries→deny→clippy→cargo test --workspace→pnpm build/test). T034 `[X]`.
- 2026-07-01: Петля продолжила без остановки → SLICE=T035 (Playwright e2e US1). **Найден и закрыт реальный инфра-пробел**: у vite dev-сервера не было proxy `/api`→gateway:3000 (браузерные запросы 404-лись бы у самого vite) — добавлен `apps/web/vite.config.ts` `server.proxy`. `playwright.config.ts` расширен до 3 процессов (`fixture-module`:4000, `gateway`:3000, `web`:5173, все `reuseExistingServer`). `POST /api/v1/registry` ещё не реализован (US2/T036+) → `tests/e2e/composed-workflow.spec.ts` сидит `service_module` напрямую SQL-инсертом через `docker compose exec db psql` (тот же паттерн, что у Rust contract-тестов через `RegistryRepository`, только с TS-стороны против реальной dev-БД; идемпотентно через `ON CONFLICT`). `test-results/` добавлен в `.gitignore`. Оракул `just e2e` зелёный с первого прогона (`1 passed`), `just test` тоже зелёный. Коммит `e01ca01` через полный husky-гейт. T035 `[X]`. **User Story 1 (MVP) ПОЛНОСТЬЮ ЗАКРЫТА** (все задачи Phase 3 `[X]`). Следующий слайс — Phase 4 US2 (T036+: `POST /api/v1/registry` handshake).
- 2026-07-01: Новая сессия, `/elt-code продолжай` → `/elt-loop` на Phase 4 (US2). SLICE=T036+T037+T040+T041 (bundled — husky pre-commit гоняет `cargo test --workspace` целиком, красный тест нельзя закоммитить отдельно, повторили паттерн T027+T031+T032 из US1). T036: контракт-тест `POST /api/v1/registry` success path — сначала красный (405, роута нет), это ожидаемое TDD-состояние (tasks.md: T036-T039 MUST fail before impl). T037: контракт-тест contractVersion mismatch → **422 Unprocessable Entity** (design-выбор по аналогии с существующим 409 для duplicate key; gateway-api.md статус не специфицировал). T040: `crates/gateway/src/registry/service.rs` — handshake (`sdk.fetch_manifest`+`sdk.check_health` уже существовали в module-sdk из прошлых сессий → просто скомпонованы), валидация против новой `SUPPORTED_CONTRACT_VERSION="1.0.0"` (`contracts::module_contract`), persist через существующий `RegistryRepository::insert`. T041: POST-хендлер + роут в `lib.rs` (`get(...).post(...)`), маппинг ошибок → 201/422/502/409/500. Оракул `just test` зелёный (9 contract + 1 integration + 2 unit + vitest). Коммит `eb42eac` через полный husky-гейт. T036/T037/T040/T041 `[X]`. Следующий слайс — T038 (DELETE contract test) + T042 (soft-delete handler), затем T039 (integration Scenario 2).
- 2026-07-01: Петля продолжила без остановки (2 слайса подряд). SLICE=T038+T042: контракт-тест DELETE + soft-delete хендлер (204/404), роут `delete()` в `lib.rs`. `just test` зелёный (11 contract). Коммит `cf0837a`. SLICE=T039(+T043): integration-тест quickstart Scenario 2 (register через реальный handshake → catalog → compose → DELETE → catalog без него → workflow/view = unavailable) — **прошёл сразу, без правок**: обнаружено, что T043 («no longer available» state для removed-модуля) уже был реализован в `aggregation/service.rs` при постройке T031 в прошлой сессии (`resolve_item`'s `is_removed()` branch, с явным комментарием-заделом «US2 T043 refines the removed-but-still-composed wording»). Отметил T043 `[X]` постфактум с пометкой в tasks.md. `just test` зелёный (2 integration). Коммит `f26c007`. **Backend US2 (T036-T043) полностью закрыт.** Остаток US2: T044 (frontend register-form) + T045 (Playwright e2e Scenario 2).
- 2026-07-01: Петля продолжила третий слайс подряд. SLICE=T044+T045: форма «Register module» (base URL → `POST /api/v1/registry`) в `Catalog.tsx`; второй независимый `fixture-module`-процесс в `playwright.config.ts` (порт 4001, `FIXTURE_KEY=e2e-onboarding-fixture` — принципиально ОТДЕЛЬНЫЙ процесс, не повторная регистрация того же самого, иначе тест не проверял бы «без core-изменений»); e2e `tests/e2e/module-onboarding.spec.ts` гоняет полный цикл register→compose→remove через UI (DELETE — через `page.request`, т.к. UI-кнопки удаления вне скоупа T044). `just e2e` зелёный с первого прогона (2/2 passed), `just test` зелёный. Коммит `09a2378`. T044/T045 `[X]`. **User Story 2 (Phase 4) ПОЛНОСТЬЮ ЗАКРЫТА** (T036-T045). Следующий фронтир — Phase 5, US3 («workflow survives one module being down», T046+).
- 2026-07-01: Юзер выбрал переключиться на демо-подготовку (P7 репетиция) вместо US3. Поднял живой стек (db+2×fixture-module на 4000/4001+gateway+web) и прогнал Scenario 1+2 через `agent-browser` вручную. **Находка 1 (исправлена, с подтверждения юзера)**: dev-БД содержала 132 мусорные записи `service_module` (owner='test-suite', накоплены за неделю прогонов `just test`/`cargo test` — тесты по умолчанию бьют в ту же `DATABASE_URL`, что и dev, нет отдельного `TEST_DATABASE_URL`) → каталог на демо-странице показывал 120+ дублей вместо 1 чистой записи. Удалено каскадно (`workflow_composition_item` → `service_module` WHERE owner='test-suite') с явного подтверждения юзера (auto-mode заблокировал первую попытку как bulk-delete). ⚠ **Повторится** при следующем `just test` — перед реальным демо либо прогнать очистку заново, либо развести `TEST_DATABASE_URL`. **Находка 2 (НЕ исправлена, юзер отложил обсуждение)**: страница совсем без CSS — голый HTML, сырой JSON текстом (см. скриншот `demo-rehearsal-final.png` в scratchpad). Функционально Scenario 1+2 отработали безупречно: регистрация мгновенно обновляет каталог без reload, workflow/view корректно показывает `unavailable: module no longer available` после DELETE. Gotcha для agent-browser+PowerShell: `@eN`-рефы ОБЯЗАТЕЛЬНО квотировать (`"@e6"`), иначе PowerShell съедает `@` как splat-оператор. Демо-стек (gateway/web/2×fixture-module) оставлен запущенным в фоне на случай если юзер захочет посмотреть сам.
- 2026-07-02: **elt-система апгрейд A-F** (чекпойнт `.planning/CHECKPOINT-2026-07-02-elt-system-upgrade-kickoff.md`). Шаг A: мёрж `feature/elt-code-judge-teeth`→`main` (`2d030ad`), мусор-файлы снесены, global `settings.json` WebSearch/WebFetch убраны из allow (юзер подтвердил), `settings.local.json` юзер решил не трогать. Шаг B: `elt-loop`→v0.2.0 (run-log, tasks.md красная линия T043, fresh-context, flake-правило, prune, судья→независимый Task-субагент) — коммит `acc26cb` в `~/.claude` repo. Шаг C: `elt-work`→v0.2.0 (`artifact-sensor.py` — механический сенсор до чеклиста, live-fire на 0-байтном файле поймал FAIL) — коммит `81062d8`; зеркала elt-loop+elt-work → `~/.codex/skills`, `~/.gemini/skills` (хирургический cp, версии сверены). **Шаг D (⚠ решение юзера): Windows Task Scheduler**, не durable CronCreate (7-дневный авто-истечок не даёт настоящую еженедельку) — задача `ELT-System-Weekly-Audit` (weekly, Mon 08:00) прогоняет расширенный `tools/elt-code-audit.js` (+3 метрики: elt-code route-line %, elt-loop run-log слайсы/красные, elt-work confirm rate) → `.planning/elt-system-audit-latest.md`. Ручной прогон подтверждён (`LastTaskResult=0`).
- 2026-07-01 (новая сессия): Факт-аудит харнесса живьём — `just test` на AWE3 живьём зелёный (после убийства зависшего демо-стека, блокировавшего файл-лок gateway.exe). Собран визуальный `SYSTEM-SCHEMA.html` (схема elt-code→elt-loop→судья, проверена agent-browser скриншотом). Найдено расхождение: `AGENTS.md`/`.gemini/GEMINI.md` несли **буквальный журнал с датами** в секции «Current State» (устаревшее упоминание claude-mem) — ровно анти-паттерн «память в CLAUDE.md». **Правка (dogfood, «эталон Бориса Черни»)**: общий движок доков `tools/project-docs-core.js` — новый `CORE_SECTIONS=[Commands,Stack,Gotchas,Memory]` (убраны Overview/Architecture-филлер; Current State→Memory, семантика = ТОЛЬКО указатель на `.planning/STATE.md`+`PROJECT-HISTORY.md`, не журнал); новая проверка `memory-leak` (датированные буллеты в Memory → WARN) через `docs-gate`/`doctor`/`doc-hygiene audit`; `doctor-core.js` больше не дублирует список (импортирует). Тесты (`project-docs.test.js`, `doctor.test.js`, `docs-gate.test.js`) зелёные. `doc-hygiene`→v1.1.0, `project-bootstrap`→v1.4.0 (эти 2 SKILL.md — ВНЕ этого репо, в `~/.claude/skills/`, не коммитятся сюда). Этот репо синхронизирован (`CLAUDE.md`/`AGENTS.md`/`.gemini/GEMINI.md` идентичны, 45 строк). `project-docs.js audit --root .` → PASS (было WARN missing-section). **Важно**: правка тулинги — не автоматическая, применяется к ДРУГИМ проектам только явным прогоном `/project-bootstrap` или `/doc-hygiene audit-all` на них (ничего не бежит в фоне).
- 2026-07-10: **T003 [live] закрыт** (specs/002-elt-fleet) — живой прогон claude -p / codex exec / agy -p в scratch-git выявил 2 неверных допущения из T002: (1) agy игнорирует STDIN как канал промпта (отвечает про свои же CLI-флаги вместо ввода) → промпт теперь передаётся значением `-p` в argv; (2) agy игнорирует `cwd` процесса без явного `--add-dir` (иначе пишет в фикс. `~/.gemini/antigravity-cli/scratch`) → добавлен `--add-dir <cwd>`. Побочно найдено: agy резолвится в реальный `.exe` на PATH (не `.cmd`-шим, как claude/codex) → спавнится БЕЗ `shell:true` (иначе argv-промпт открыл бы shell-injection). agy-таймаут = exit 1 + "Error: timeout waiting for response" (не hang). Реальный 429 живьём не словили (эвристика в router.js осталась, комментарий уточнён честно). Правки в `tools/fleet/providers.js`+`router.js`, оракул 56/56, судья pass, коммит `4e667df`. Осталось T016 (бенч) + T017 (драки) → вердикт v1 → merge в main.
- 2026-07-06: **Prompt Party доклад (Harness+Loop eng) СОБРАН** — 20-мин Zoom-доклад, ОТДЕЛЬНЫЙ от `presentation/index.html` (тот = нетехнарь-дек начальника, не трогать). Собрано 2 файла в `presentation/`: **`harness-loop-talk.html`** (14 слайдов; теория начальника из `prompt_party_materials/part1|part2` с встроенными hand-drawn диаграммами → наша box-схема с тегами компонентов каркаса + таблица-мапинг 12 компонентов→наша реализация → реальный кейс AWE3 US2 → карта живого демо; сквозная фраза «Якщо ти не модель — ти каркас, последнее слово о качестве — за машиной») + **`DEMO-RUNBOOK.md`** (тайминг + 5-мин префлайт + 4 акта живого показа с точными командами + fallback). Рендер верифицирован agent-browser (сл. 3/9/13), JS валиден, картинки резолвятся. **AWE3 переехал → `C:\Ametrin projects\Ametrin ecosystem old\Ametrin web ecosystem 3`** (старый путь мёртв); ветка `feature/us1-slice2-aggregation`, оракул `just test`, гейт husky 8-сенсоров, кейс T038/`cf0837a`, 11 открытых задач от T046, тулинг весь стоит. Полный стек НЕ поднимал (засоряет dev-БД). **Оба файла untracked, в git не коммичены.** Открытые решения (спросить юзера): (1) коммитить ли `presentation/` в git; (2) Акт 4b (живой `/elt-loop` на T046) оставить/убрать; (3) предложено прогнать Акт 3 fmt-reject живьём. Чекпоинт: `.planning/CHECKPOINT-2026-07-06-prompt-party-talk-built.md`.
