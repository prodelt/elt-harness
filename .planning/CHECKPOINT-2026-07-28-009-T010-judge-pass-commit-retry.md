# CHECKPOINT 2026-07-28 — 009 T010: судья PASS, коммит на повторе (treeHash протух от хука)

**Спека:** `specs/009-elt-v3-thinking-harness` · **ветка:** `feature/judge-bench-parallel-oracle` · **HEAD:** `232b209`
**Состояние:** T010 дописан, оракул 58/58, судьи **pass** — но `elt commit` отверг proof (`stale-tree`).
На момент записи чекпоинта в фоне идёт повторная цепочка `oracle → judge run → commit`.

## Что решено и сделано в этой сессии
Блокер прошлой сессии (verify-судья codex трижды `dead`) закрыт **failover'ом verify-судьи**
(юзер выбрал этот вариант из трёх; альтернативы — verify на claude в конфиге, парковка T010):

- `tools/fleet/gate.js` — `JUDGE_TIMEOUT_MS` **откачен 9→5 мин** (гипотеза «мало времени» опровергнута
  замером: 301/301/**540**с, а на коротком промпте тот же CLI отвечает за секунды → codex висит на
  крупном промпте). Вместо этого: мёртвый verify-судья (`runOk:false`) **перевыдаётся следующему
  доступному CLI**, не совпадающему ни с первичным судьёй, ни с мёртвым, ни с воркером (`judgeExclude`).
  Мёртвая попытка остаётся в `judges[]` — след в proof, не тихая подмена.
- `tools/fleet/providers.js` — `available(provider)` (резолв `FLEET_BIN_*`/`where` с кешем) переехал
  сюда из `fleet.js`: нужен обоим. `JUDGE_ALTS` теперь один список (`gate.JUDGE_ALTS`), не дубль.
- `tools/fleet/fleet.js` — прокидывает `judgeExclude: [provider воркера]` в оба вызова `gate.gate`.
- `tools/judge-verify.test.js` — **зона расширена 5-й раз**: его кейс «второй судья мёртв» теперь
  значит «мёртв И заменить некем», и незастабленный codex считался живой альтернативой (тест
  спавнил РЕАЛЬНЫЙ codex, 87с). Стаб расширен на все 3 CLI → файл 87с → 2.3с.
- `specs/009-.../tasks.md` — из T010 удалено опровергнутое утверждение про 9-минутный таймаут,
  вписан фактический замер и что сделано вместо.

## Доказательства
- Оракул **58/58**, 180с (было 240с — минус реальный codex из judge-verify.test.js).
- Судья **pass**, и новый failover **отработал живьём**, ровно в том месте, где раньше был judge-dead:
  `agy pass (38с)` → `codex dead (300с)` → **перевыдача `claude/sonnet` → pass (265с)**.
  claude-судья прогнал 70/70 тестов сам. `redProof.status: "red"` (fails-on-base), `attested: true`.
  Пруф-лог сессии: `.git/elt/run-log.jsonl`, запись `12:51:02Z judge-pass`.

## Почему коммит не прошёл с первого раза (две ловушки подряд)
1. **Коллизия ID** (уже была в прошлом чекпоинте, наступили снова): `elt commit --task T010` БЕЗ
   `--spec` связался с `specs/006-elt-front-gate` → «спека не утверждена (stale, 006)», exit 4.
   Всегда `--spec specs/009-elt-v3-thinking-harness` — и в `judge run`, и в `commit`.
2. **`stale-tree`**: хук авто-чекпоинта (UserPromptSubmit, порог токенов) перезаписал
   `.planning/CHECKPOINT-2026-07-28-auto.md` ПОСЛЕ того, как судья зафиксировал `treeHash` —
   proof законно отвергнут. Уведомление о фоновой задаче тоже триггерит этот хук, поэтому
   гейт и коммит надо гнать **одной цепочкой в одном фоновом вызове**
   (`oracle && judge run && commit`), не отдельными ходами.

## Третий прогон гейта (чистая цепочка) — оракул 58/58, но судья `dead`
Оракул зелёный (58/58, 192с). Судьи: `agy pass 23с` → `codex DEAD 300с` → **failover** →
`claude/sonnet DEAD 302с`. Кандидаты кончились → `verdict: dead`, коммита нет.
Механика T010 сработала верно (перевыдача состоялась), но оба verify-CLI не уложились.

**Замер разводит причины — их ДВЕ, а не одна (в `tasks.md` сейчас записана одна общая, неточно):**
- `codex/gpt-5.6-sol`: 301 / 301 / 540 / 300с — временем НЕ лечится, висит на крупном промпте.
- `claude/sonnet`: **265с → pass**, затем **302с → таймаут**. Ему времени реально не хватило
  чуть-чуть: 5-минутный лимит для него впритык на диффе этого слайса (~60K символов).

## РЕШЕНИЕ ЗА ЮЗЕРОМ (нужно явное «утверждаю» — это правка текста задачи)
Варианты закрыть гейт T010:
1. Поднять `JUDGE_TIMEOUT_MS` 5→8 мин: claude уложится (~5 мин), codex по-прежнему умрёт впустую,
   но детерминированно. Цена — гейт ~14 мин. Формулировку в `tasks.md` придётся переписать:
   «гипотеза таймаута опровергнута» верно для codex и НЕВЕРНО для claude.
2. Убрать codex из `judge.verify` в `.harness/harness.json` (verify → claude) — снимает 5 мин
   впустую с каждого гейта, но жжёт Claude-лимит всегда и расширяет зону на конфиг.
3. Резать промпт судье (дифф 60K) — новая работа, не T010.

## ДАЛЬШЕ (после решения)
1. Правка → `elt spec approve --spec specs/009-elt-v3-thinking-harness` → **одна фоновая цепочка**:
   `elt oracle && elt judge run --task T010 --spec ... && elt commit --task T010 --spec ... --skip-oracle`.
2. После T010: **T011** (живой прогон, закрывает спеку), затем T012/T013 (фаза F, ускорение).
3. Отдельно, вне 009: **codex/gpt-5.6 висит на крупном промпте** — не блокер (failover есть),
   но не диагностирован. Кандидат в отдельную задачу.
