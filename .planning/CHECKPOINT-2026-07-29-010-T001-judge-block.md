# CHECKPOINT 2026-07-29 — спека 010 создана и утверждена, T001 заблокирован судьёй по делу

**Ветка:** `feature/judge-bench-parallel-oracle`. **HEAD:** `d5c8e1f`-серия (см. `git log`).
**Статус:** спека 010 утверждена и закоммичена; T001 сделан по существу, но НЕ закоммичен — block.

## Сделано

- **grill-me, 2 раунда**, все 4 категории закрыты. Решения Р1–Р7 в `specs/010-judge-delivery/spec.md`.
- **`specs/010-judge-delivery/`** — spec.md (+mermaid) и tasks.md (11 слайсов), `elt spec lint: ok`,
  юзер сказал «утверждаю», `elt spec approve` выполнен, закоммичено `97143b1`.
- **T001 (разведка D0)** — `.planning/D0-smoke-feasibility.md` + `tools/d0-smoke-feasibility.test.js`
  (2/2 зелёные). Вывод `N=2`. **Не закоммичен — судья дал block.**

## Разведанные факты (поправки к аудиту 2026-07-29)

1. **`fleet.js:182` уже починен** в `28aa4b7` (T010): `judgeAwayFrom` → `null` → парковка
   `judge-unavailable`. Пункт «НЕ ПРОВЕРЕНО #3» прошлого чекпоинта устарел.
2. **Поверхность — 9 проектов, не 122.** `.harness/harness.json` есть у: `Pipiline setupper`
   (мост есть), `Ametryn_protocol_bot` ×3, `project_social_analysis`, `Route_API_1C`,
   `Marketing_tg_bot`, `pdv`, `tg_bot_reclamaties-master`, `Ametrin web ecosystem 4`.
3. **Замыкание моста — 8 файлов:** `judge-invoke.js` → `fleet/gate.js` →
   `fleet/{providers,exec,plan,router}.js` + `red-proof.js` + `elt-config.js` (последний уже в bin).
4. **Копия, а не шим на репо:** fleet исполняет слайсы в git worktree, путь к репо нестабилен.

## Блок T001 — вердикт судьи (по делу, принять)

Оракул зелёный. `agy` — pass. `codex` (verify) — **block**:
- `N=2` выведен рассуждением по git-истории, без воспроизводимой проверки: нет точных команд,
  входов, ассертов и зафиксированного красного прогона на дефектной версии.
- Тест валидирует форму отчёта (3 секции, парсинг `N=`), а не истинность вывода: останется
  зелёным, если содержание заменить произвольной прозой.

**Что нужно для снятия блока** — настоящий red-proof минимум для двух зачтённых регрессов:
- Регресс 3 (Route_API_1C): `git checkout ce71a9f^`, живой прогон геокодера с `VISICOM_API_KEY`,
  показать что Visicom проигрывает Geoapify, и что предлагаемый smoke-ассерт краснеет.
- Регресс 2 (doc2md-tauri): состояние `216e364`, живая конвертация с ключом Gemini, показать
  счётчик токенов = 0 и красный ассерт.

Альтернатива, если живые ключи недоступны: доказательство прямо из затронутого кода и
фактического ответа внешнего API (сохранённый ответ + разбор парсера).

## Гочты сессии (обе стоили по ~3.5 мин)

- **Пруф оракула пишет `elt oracle`**, а не `node tools/elt-oracle-runner.js` напрямую.
  Правильная цепочка одним фоновым вызовом:
  `elt oracle && elt judge run --task Txxx --spec specs/010-judge-delivery && elt commit --task Txxx --spec specs/010-judge-delivery --skip-oracle`
- **Хук авто-чекпоинта** пишет `.planning/*-auto.md` в дерево и протухит пруф (`stale oracle proof`).
  Коммитить его ДО цепочки. Это D3 живьём — слайс T009 спеки 010.
- **Карантин: `tools/context7-cli.test.js`** ходит в сеть и флейкует в оракуле (1 красный из 4
  прогонов, без единой правки в нём).

## ДАЛЬШЕ (Resume)

1. **Снять блок T001**: собрать воспроизводимый red-proof по требованию codex (см. выше) ИЛИ
   переформулировать T001 честнее — например, вывод «N=2» понизить до гипотезы, а доказательство
   вынести в отдельный слайс с живым прогоном. Второе дешевле и не врёт.
2. **Дописать smoke-слайсы** в `tasks.md` (поле `smoke` в `harness.json`; команду объявляет
   проект, не система; пропуск без ключей — ГРОМКИЙ в run-log) → показать юзеру → «утверждаю»
   → `elt spec approve` заново.
3. **Слайсы по порядку**: T002 `sync-bin.js` → T003 fallback-резолв `elt.js:606` → T004 doctor
   WARN → T005 удалить `--skip-attest` → T006-T008 project-bootstrap → T009 хук → T010 скиллы
   → T011 живая приёмка в `C:/Ametrin projects/Route_API_1C`.
