# CHECKPOINT 2026-07-31 — спека 011 ELT v3 УТВЕРЖДЕНА, resume T001

## ЗАДАЧА
Редизайн гейта ELT v3 по `.planning/ELT-V3-DESIGN-BRIEF.md`. Грилл (Режим 0) проведён,
`specs/011-elt-v3-gate/{spec.md,tasks.md}` написаны, `elt spec lint: ok`,
**`elt spec approve` выполнен** (юзер сказал «утверждаю» 2026-07-31):

    specHash  21b37eec47c7d5dc90ad96444a496844b2c9622480cc9d8200003a8ea8a8dfdd
    tasksHash d0fcae8b4bdd09392f6059e5d8e8ed796488856b2a11d9bbde1ec0054fc4c491

## РЕШЕНИЯ ГРИЛЛА (2 раунда, 8 вопросов, все рекомендации приняты)
1. Риск-триггер = **фиксированный список**, не обученный скор (калибровать нечем: первичных
   блоков в истории 7, а 36/48 блоков — verify-шум; обучение на нём выучит шум).
2. `inconclusive` = **неблокирующая очередь** `.harness/review-queue.jsonl` + разбор пачкой
   (`elt review`). Второго раунда судьи на том же слайсе НЕТ — осцилляция исключена физически.
3. L2 smoke = **`harness.json.smoke`**, строка команды по форме `oracle`; нет поля → нет слоя.
4. Качество тестов = **свой мутатор по ИЗМЕНЁННЫМ строкам** (~100 строк stdlib), не Stryker.
   Обязательное предусловие — L1 impact-выборка. Другие стеки: поле есть, слой выключен.
5. Context7 = **пруф-файл в репо** `.harness/ctx7-proof.jsonl` (пишет `tools/context7-cli.js`)
   + L0-блок на новый внешний импорт без свежего пруфа.
6. Люк `--attested-by` **удаляется целиком**, fleet пишет proof in-process (не подпись цепи).
7. Порядок = **dogfood с первого слайса**: T001 (снять verify) → хвост 010 → остальное 011.
8. Хвост 010 (T005/T009/T010/T011) **переезжает в 011** как T011/T012/T013/T014.

## РАЗВЕДАНО КОДОМ (не спрашивалось у юзера)
- `stash@{0}` = `batch2-T005-T009-T010-T011` — второй батч 010 **цел**, не потерян.
- Причина блока 29.07: primary `claude/sonnet` дал **pass**, блокировал verify `codex` по двум
  причинам — (а) `elt.js:675` принимает литерал `fleet-gate` от ЛЮБОГО вызывающего (блок **по
  делу**, стал решением 6); (б) «части диффа обрезаны» = `DIFF_CAP` на батче из 8 слайсов →
  REJECT-default. Второе — не качество, а ровно чинимый дефект. **Батчи из 8 не повторять.**
- `red-proof.js:60` → `skipped: no-new-tests`: правка СУЩЕСТВУЮЩЕГО теста проскакивает слой целиком.
- `elt-oracle-runner.js:discover` берёт все `tools/**/*.test.js` всегда, параллельно.
- run-log: 270 записей, есть `commit`/`durationSec`/`tokens`/`costUsd`/`judges[]`, **нет размера
  диффа** — реконструируется из git по `commit`.
- `judge-bench.js` уже умеет `--out` с JSON-отчётом (писать раннер не нужно).
- В репо **нет `package.json`** — нулевые зависимости, отсюда «свой мутатор, не Stryker».
- `~/.claude/ctx7-cache/access.log` — машинный след был, последняя запись **2026-06-17**: писал
  снятый 18.06 AMOS-хук. Механизм мёртв, воскрешать не стали.
- Гочта прогона тестов: оракул гоняет `node <file>`, а НЕ `node --test <file>` — второе висит
  бесконечно на этих файлах (потерял ~10 мин).

## СОСТОЯНИЕ ДЕРЕВА
Закоммичено этим чекпоинтом: `specs/011-elt-v3-gate/` (spec + tasks + approval) + этот файл.

**Осталось грязным намеренно** — хвост 010, написан и **зелёный (3/3 PASS: `doctor.test.js`,
`project-bootstrap.test.js`, `project-docs.test.js`)**:
- `tools/doctor-core.js` + `doctor.test.js` — 010/**T004** (WARN на дрейф моста).
  ⚠ Содержит ЛИШНИЙ `checkLoopJudgePath` (инцидент 30.07 «Задача фузи музи») — **вне зоны T004**,
  вынести отдельным слайсом или явно расширить `[files:]`, иначе судья законно поймает scope creep.
- `tools/project-bootstrap.js` + тест — 010/**T006** (контракт `judgeBridge`) и **T007** (оракул
  исполняемый).
- `tools/project-docs-core.js` + тест — 010/**T008** (шум вон из красного).
- `specs/010-judge-delivery/approval.json` — stale-hash после `[X]` T002/T003, перевыпустить
  `elt spec approve --spec specs/010-judge-delivery` при возврате в 010 (diff tasks.md — чистый
  `[ ]`→`[X]`, тихий re-approve разрешён).

## ДАЛЬШЕ (строго в этом порядке)
1. **011/T001** — убрать `judge.verify` из `.harness/harness.json`; прогнать
   `node tools/judge-bench.js --out .planning/JUDGE-BENCH-011-T001.json` одиночным первичным
   (`agy`/`gemini-3.6-flash-high`, 14 кейсов); контракт-тест — `verifySettings` для корня
   возвращает `null` + структура отчёта парсится. Диффа на одну строку конфига — пройдёт даже
   текущим двойным гейтом.
2. **Хвост 010** — T004, T006, T007, T008 **по одному слайсу**, уже облегчённым гейтом.
   Это закрывает спеку 010 целиком.
3. **011/T002+** — L0-механика и дальше по `specs/011-elt-v3-gate/tasks.md`.

## ГОЧТЫ, КОТОРЫЕ УЖЕ СТОИЛИ ВРЕМЕНИ
- Гейт гнать **одной фоновой цепочкой** oracle→judge→commit: любая запись в дерево между шагами
  (в т.ч. авто-чекпоинт-хуком) = `stale-tree`, прогон судьи 10–18 мин впустую. Всегда `--spec`.
- `elt spec approve` **пишет в дерево** — делать ДО судьи, не между судьёй и commit.
- agy-судья падает `spawn ENAMETOOLONG` на больших диффах (промпт через argv). Обход:
  `elt judge run --task Txxx --provider claude --model sonnet`.
- `tools/elt.js` ≡ `~/.claude/bin/elt.js` — синхронить вручную.
