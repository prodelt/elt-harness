# Checkpoint — 2026-06-20 — ELT-CODE: A доказан, старт B (eval-флайвил)

## Build Status
- n/a (docs/infra-репо). Инструмент шва: `node tools/update-judge-verdicts-index.js --self-check` → OK.

## Где мы
**Режим A `/elt-code` СОБРАН (Фазы 0-4) и ДОКАЗАН реальным прогоном.** Спека: `.planning/ELT-CODE-DESIGN.md`.
- Диспетчер (6 интентов) + судья-гейт (H0-H2 блок / S1-S4 совет, sonnet/opus-субагент, инлайн в не-Claude)
  + spec-kit per-project + кросс-CLI зеркало (codex/gemini) + индекс `tools/update-judge-verdicts-index.js`.
- **Критерий 5 режима A закрыт** (ELT-CODE-DESIGN.md:168): первый реальный end-to-end прогон на живом
  проекте **Fasoli 2.0** — Track A (hardening 4 скраперов) прошёл судью **PASS**, смержен (PR #18 → main `78086f1`).
- **Шов A→B доказан на реальных данных:** первый реальный `judge_verdict` записан в КАНОН-локацию
  `~/.claude/projects/fasoli-2.0-fbdaaf37/session-ledger.jsonl`; канон-скан индексатора подхватил его
  БЕЗ ручного аргумента (1 verdict, COMPLEX, pass, S-avg 4.8). См. [[project_elt_code_design_2026-06-20]].

## Что такое B (из дизайна — пока ОДНА строка, не спека)
> «**B — eval-флайвил** (система измеряет и улучшает себя). Заводится на данных, что копит A.»
> «Шов A→B = лог судьи `judge_verdict` в session-ledger. Накопленные вердикты = eval-корпус для B.»

Отдельного дизайн-дока B НЕТ. Первый шаг нового чата = **спроектировать B** (grill-сессия, как прошёл A),
НЕ сразу кодить. Дизайн B не требует толстого корпуса; полноценная работа B — требует (сейчас поток
только начался).

## Открытые вопросы дизайна B (вход в grill)
1. **Что именно измеряет флайвил?** Тренды block% / S-avg по проектам / времени / моделям? Дрейф
   качества? Recall судьи (ловит ли реальные проблемы)?
2. **Что значит «улучшает себя»?** Правит рубрику судьи? Правит скилы (`/elt-code`, диспетчер)?
   Предлагает юзеру? ⚠ **Критичная рамка: «самоулучшение» = прямой риск рецидива AMOS** (авто-инъекция,
   авто-loop, advisory-спам — то, что снесли 2026-06-18). B обязан быть **on-demand + advisory**, а не
   автономный цикл правок. Это перило №1 дизайна B.
3. **Eval чего?** Качество КОДА через вердикты, или мета-eval самого СУДЬИ (судит ли он верно)? Или оба слоя?
4. **Инструменты анализа поверх корпуса.** Семя уже есть: `tools/update-judge-verdicts-index.js`
   (агрегаты: total, block%, S-avg, распределение моделей). Что добавить?
5. **Петля measure→improve:** кто/что вносит изменение и где гейт ревью человеком?
6. **Порог зрелости:** при каком объёме корпуса B даёт надёжный сигнал (сейчас 1 реальный — мало)?

## Текущий корпус (семя B)
- Канон (`projects/*/session-ledger.jsonl`): **1 реальный** вердикт (Fasoli, pass).
- Demo (`~/.claude/tmp/elt-judge-livefire/`): 5 live-fire вердиктов Фаз 1-2 (НЕ канон-скан — только с
  явным аргументом). Полезны как фикстуры для тестов инструментов B, не как реальный сигнал.

## Git State
- Pipeline Setupper: ветка `feature/doc-hygiene-phase2`. Этот чекпоинт + индекс — некоммичены.
- Fasoli 2.0: на свежем `main` (`78086f1`, Track A смержен). Track B Fasoli (Liquid Glass C0-C3,
  ветка `feature/c0-sdk54-glass`, 21 коммит, гейты зелёные) — НЕ запушен, отдельный от системного B.

## Resume Pointer
- **Focus:** спроектировать **B (eval-флайвил)** режима `/elt-code` — grill-сессия по 6 открытым
  вопросам выше; перило №1 = on-demand+advisory, НЕ авто-loop (анти-AMOS).
- **Resume (новый чат, cwd = Pipeline Setupper):** старт-промптом ниже. Прочитать `.planning/ELT-CODE-DESIGN.md`
  (секции «Рамка», «Отложено»), этот чекпоинт, `tools/update-judge-verdicts-index.js` (семя инструментов).
- **Старт-промпт для нового чата:**
  `Проектируем B (eval-флайвил) режима /elt-code. Прочитай CHECKPOINT-2026-06-20-elt-code-B-kickoff.md
  и проведи grill-сессию по 6 открытым вопросам. Перило: on-demand+advisory, не авто-loop (анти-AMOS).`
