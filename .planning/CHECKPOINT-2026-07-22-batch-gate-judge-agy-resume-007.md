# CHECKPOINT 2026-07-22 — батч-гейт + судья agy + elt в трёх CLI

Ветка `feature/judge-bench-parallel-oracle`. Коммиты этой сессии:
`e3790f7` (батч-гейт), `c2f70e0` (.gitignore рантайм-логов) — репо;
`53f0ffb` — `~/.claude` (CLI + SKILL.md).

## Сделано

**1. Батч-гейт — единица гейта = батч, а не задача.**
- `elt commit|judge-proof write --task T001,T002,T003` — proof связан составом ЦЕЛИКОМ
  (другой состав/подмножество → отказ; любая задача не открыта → отказ).
- `elt slice next --json --count N` (при `--count 1` форма вывода не изменилась — объект).
- `elt-loop.ps1 -Batch N` / `harness.json → "batch": N` (дефолт 1 = старое поведение):
  свежий `claude` на КАЖДУЮ задачу, гейт один на батч.
- Промпт судьи: батч судится по каждой задаче отдельно; мок-тест, подменяющий
  проверяемую логику, объявлен НЕ доказательством (то же в промпте имплементатора).

**2. Судья репо → `agy` / `gemini-3.6-flash-high`** (`harness.json`). Перемер judge-bench
после правки промпта: agy **9/9 (100%)**, медиана 42с, подписка Antigravity;
codex gpt-5.6-sol **8/9** — ложный block на `clean-large-in-scope` («объём ≠ scope creep»),
медиана 38.5с. Юзер хотел codex — данные против; переключение = одна строка конфига.

**3. elt в трёх CLI — проверено живьём.** Свежий `SKILL.md` синхронизирован в
`~/.codex/skills/elt/` и `~/.gemini/skills/elt/` (отставали: учили мёртвой команде
`elt commit --verdict`, которую CLI отвергает exit 4). В ГЛОБАЛЬНЫЕ `~/.codex/AGENTS.md`
и `~/.gemini/AGENTS.md` добавлен явный указатель на файл протокола + ядро CLI (agy не
читает `~/.gemini/skills` вовсе). Проверка: и agy, и codex прочитали указатель и выдали
верные команды батча. Хуки-зубы: Claude Code ✓, Codex ✓ (видны PostToolUse/Stop), agy ✗.
⚠ `~/.codex` и `~/.gemini` НЕ под гитом — правки без версии.

## Доказательства
- `node --test tools/elt-batch.test.js` — 4/4 (реальный git + реальный `elt commit`, не стабы)
- полный оракул `node tools/elt-oracle-runner.js` — **46/46**, 218с
- `-DryRun -Batch 3` — поймал баг PS5.1: `ConvertFrom-Json` отдаёт массив ОДНИМ объектом,
  `@(...)` даёт массив-из-одного → батч склеивался в один промпт; фикс = `foreach`-statement
- живой судья agy на диффе этой сессии: `runOk:true, verdict:pass`
- `.harness/STOP` снят — драйвер снова рабочий

## Честная оценка скорости
Батч даёт **≈1.2×**, не «в разы»: по 77 закрытым слайсам гейт = 24% времени слайса,
имплементатор = 76%. «В разы» дадут только параллель (fleet / несколько agy) или более
мелкие слайсы — не выпиливание гейтов.

## Resume (следующая сессия)
Роадмап живьём: 001-004 закрыты; **005** 20/23 (T021 можно делать; T022/T023 —
внешние блокеры: Ametryn A/B + pilot approval); **006** 9/19 (T009-T018, approval `stale` —
сверить дифф `tasks.md`: чистый `[ ]`→`[X]` → тихий re-approve, иначе спросить юзера);
**007** 0/3 (микроплан без `spec.md` → approval не нужен, `slice next` работает).

Кандидаты, в порядке предпочтения:
1. **007 одним батчем** (3 `[P]`-слайса, `[cli:agy]`): judge-bench +4 дефекта, **e2e-тест
   реального гейта вместо стабов**, judge-bench-compare. Первая живая обкатка батча.
2. **006 T010** `implModel`/`implEffort` в `harness.json` — прямой рычаг скорости (те 76%).
3. Параллель несколькими agy = это fleet (модель в роутере уже актуальная).

Запуск: `powershell -File tools\elt-loop.ps1 -Project . -SpecDir specs/007-harness-speed -Slices 3 -Batch 3`
