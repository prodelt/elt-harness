# CHECKPOINT 2026-08-01 — 011/T017 закрыт; следующий шаг: включить smoke и проиграть 3 регресса

Предыдущий: `.planning/CHECKPOINT-2026-07-31-011-T003-T013-closed.md`.
Ветка `feature/judge-bench-parallel-oracle`, дерево чистое, HEAD `02f4c05`.

## Что было в этой сессии

Сверка кода и живых данных с артефактом-протоколом
(`ELT v3 — протокол замера и три схемы харнесса.html`, он же
`claude.ai/code/artifact/d5632c82-8e6b-4223-95d7-3b8d7cb46072`). Из неё родился T017 — закрыт
живым гейтом (`02f4c05`, оракул 68/68 за 182 c, судья `claude/sonnet` pass 237 c, red-proof red).

### T017 — три дефекта, найденные сверкой

| | Дефект | Фикс |
|---|---|---|
| а | `harness.json.judge.provider: "agy"` — падает `ENAMETOOLONG`; все 11 слайсов 011 шли ручным `--provider claude`, дефолтный путь репо был сломан | `claude`/`sonnet` + контракт-тест `testRepoJudgeProviderIsAlive` |
| б | Мёртвый первичный судья валил гейт: `gate.js:578` возвращал его как есть, перевыдача жила ТОЛЬКО в ветке `verify`, снятой в T001 → одна точка отказа | перевыдача по `JUDGE_ALTS`; мёртвая попытка остаётся в `judges[]` |
| в | L0 звался внутри `runJudge` (после оракула), хотя схема спеки — `S → L0 → L1` | `preOracleL0` в `elt.js` перед `runOracle` в `oracle` и `commit` |

Границы держатся специально: pre-oracle ловит **только `block`** (`inconclusive` остаётся
неблокирующим, иначе R5 сломан); `judgeNeeded` оракул не отменяет — L1 гоняется всегда.

Найдено живьём (dogfood, оба записаны в `tasks.md` T017):
1. `require('./elt-gate-l0')` в `elt.js` убивал `elt oracle` MODULE_NOT_FOUND ВО ВСЕХ проектах —
   `elt.js` живёт ещё и деплоем в `~/.claude/bin/`, где соседей-модулей два. Поймал ОРАКУЛ
   (первый прогон 65/68), не рассуждение. Резолв — фолбэком на `~/.claude/bin/judge/`.
2. Три теста стабили только `claude`(+`codex`), выражая «живого судьи нет»: до третьего CLI было
   не дойти. С перевыдачей ушли бы на РЕАЛЬНЫЙ `agy` (26 c спавна, чужой вердикт). Добавлен
   третий стаб.

## Открытые пункты сверки с артефактом (важнее сделанного)

Разобранные — в теле T017. Остальные, по убыванию важности:

- **L2 smoke написан (T010), но нигде не включён, и D0 НЕ ДОКАЗАН.** Поля `smoke` нет в
  `harness.json` этого репо; в `tools/fleet/gate.js` smoke не вызывается вовсе — только в
  `elt.js:330` (`runOracle`), т.е. fleet-воркеры этот слой не проходят. Артефакт, шаг 4,
  проверяемый результат: «три уехавших регресса воспроизводятся и ловятся — 3/3». Не сделано;
  AC10 закрыт юнит-тестами на три ветки конфига. **Это единственное обоснование тезиса «защита
  стоит не в том месте» — и оно недоказано.** ← СЛЕДУЮЩИЙ ШАГ, юзер попросил явно.
- **Калибровка (AC1/R8) измеряет НЕ того судью, что работает.**
  `.planning/JUDGE-BENCH-011-T001.json` = `agy`, recall 1, FPR 0, 14 кейсов. В проде —
  `claude/sonnet`, в этой роли на новом наборе не мерился. Плюс шаг 1 артефакта требовал
  «расширив набор ИСТОРИЧЕСКИМИ блоками» — не сделано: pass-кейсов в наборе **всего 3**, FPR по
  трём кейсам неизмерим, а FPR — корень (77%).
- **Мутатор (US3/AC8) не в гейте** — задокументировано намеренно, но задачи «включить с замером»
  в плане нет. US3 сейчас не выполняется ничем.
- Из артефакта не попало ни в задачи, ни в «вне scope»: **waggle** (ссылка на артефакт вместо
  копии в argv — «снимает ENAMETOOLONG как класс», §06); **scope по codegraph как L0-триггер**
  (артефакт: он ловит те 7 «настоящих» блоков механически); **шаг 0 — базовая линия одной
  командой** из run-log; **топология `[P]` из графа** (fleet).
- Корректно вынесены вне scope: схема B, слой эволюции (C), fleet A/B, HMAC (отвергнут решением 6
  по существу — люк удалён целиком).

## Живые числа для T015 (замер) — брать отсюда, не из витрины

- `l0-clean` фактически **4 из 13**, а не 70%. Триггеры: `existing-test-modified` 7,
  `hot-path` 5. Причина структурная: слайс по TDD почти всегда правит существующий тест-файл, и
  триггер съедает нормальную практику, а не риск. У `red-proof` (T007) и мутатора (T008) уже есть
  механика, отличающая «тест изменён» от «тест ОСЛАБЛЕН» — триггер можно сузить.
- Блок-рейт нового контура **1/13 ≈ 8%** против baseline 77%. Единственный блок — структурный
  дефект red-proof (внешний предмет). Настоящих качественных блоков 0. Выборка мала, все слайсы
  делал один автор по свежей спеке — так и писать в вердикте.
- Оракул: полный ряд `durationSec` после 17:00 31.07 —
  `151,150,150,146,151,147,154,158,95,2,153,155,180,177,1,110` → **медиана ≈ 150 c** против
  baseline 185 c. Выборочные удачные точки (1.6 c, 95 c) картину льстят. Fallback «все»
  срабатывает, как только в диффе `.harness/harness.json` или хаб `elt.js`.
- `inconclusive` живьём **ни разу (0/13)**, очередь пуста → порог R3 в AC13 объявляется на пустой
  выборке; сказать это честно, а не выдать «0% = отлично».

## Гочты, подтверждённые в этой сессии

- **`tools/elt.js` синхронить в `~/.claude/bin/elt.js` ВРУЧНУЮ** (`cp`), `sync-bin.js` кладёт
  только замыкание судьи в `~/.claude/bin/judge/`. Иначе `doctor.test.js` красный (DRIFT).
- Любой новый `require()` в `elt.js` обязан быть доступен и деплой-копии — она видит только
  `elt-config.js`, `run-log.js` и `judge/`.
- Тест с премисой «судья мёртв» обязан стабить **все три** `FLEET_BIN_*`.
- Порядок цепочки жёсткий: `spec approve → oracle → judge run → commit --skip-oracle`; между
  шагами в дерево не писать. Артефакт `.html` в корне и авто-чекпоинт заглушены через
  `.git/info/exclude` / `git checkout` ДО цепочки.

## Проект для живого smoke — решение юзера 01.08

**`C:/Ametrin projects/Задача фузи музи`** (не Route_API_1C — тот отклонён явно).

- **Что это:** Ametrin Web — веб-приложение сопоставления номенклатуры фармпрепаратов
  (fuzzy matching + AI-вердикты LLM). **Flask, `app.py`, порт 5000**, `docker-compose up --build -d`
  либо ручной запуск. Есть `wsgi.py`, `templates/`, `static/`, `uploads/`, `output/`.
- **Оракул у них:** `$env:PYTHONIOENCODING='utf-8'; $env:PYTHONUTF8='1'; pytest tests/ -p no:capture`
  (shell `powershell`). 17 тест-файлов в `tests/`.
- **`harness.json`:** `judge.provider: claude/sonnet`, **`judge.verify: codex/gpt-5.6-sol` ещё
  жив** — двойной судья, тот самый, что мы сняли у себя в T001; `attest: true`,
  `specApproval: true`, `ctx7Gate: "warn"`, `redProof: "on"`.
- **Где они сейчас:** спека `004-silent-false-pairs`, HEAD `9f41d8b` («зони мовчазних вердиктів
  закриті»). Спеки 001-etl-correctness → 002-matching-recall → 003-precision-recall → 004.
  Свежее рядом: персистентный кеш вердиктов, гейт размера, A/B верификатора.
- **Почему он подходит под D0 лучше:** оракул = pytest над функциями, а продукт — Flask-морда,
  куда человек грузит файл и получает свод. Ровно тот разрыв, ради которого L2 smoke и придуман:
  «запусти то, чем пользуется человек».
- **Кандидат на smoke:** поднять приложение и прогнать главный путь (загрузка тестового файла из
  `Файлі для теста`/`dummy` → свод), а не только `GET /`. Форма поля — строка команды, как
  `oracle`.

**Открытые вопросы к следующему чату (спросить юзера, не решать самому):**
1. Три регресса из аудита 29.07 — OCR, счётчик токенов, Visicom. «Счётчик токенов» ложится на
   этот проект (AI-вердикты), **Visicom — геосервис, это Route_API_1C**, OCR неочевиден.
   Т.е. три регресса, возможно, РАЗМАЗАНЫ по двум проектам. Проверить по
   `.planning/CHECKPOINT-2026-07-29-elt-quality-regression-audit.md` ПЕРЕД тем, как обещать 3/3.
2. **AC12 в `spec.md` 011 называет `Route_API_1C`** как проект приёмки (T014). Если приёмку тоже
   вести здесь — это правка спеки и переутверждение (`specApproval: true`).
3. Правки в чужом проекте — нужно разрешение юзера (в прошлый раз auto-mode отклонил дважды).

## Resume Prompt

> Продолжаю Pipeline Setupper, ветка `feature/judge-bench-parallel-oracle`, HEAD `02f4c05`.
> Читай `.planning/CHECKPOINT-2026-08-01-011-T017-resume-smoke.md`.
>
> Задача: **включить L2 smoke и проиграть уехавшие регрессы живьём** (артефакт-протокол, шаг 4;
> это единственное недоказанное обоснование тезиса D0 «защита стоит не в том месте»).
>
> **Живой проект — `C:/Ametrin projects/Задача фузи музи`** (Ametrin Web: Flask на порту 5000,
> сопоставление номенклатуры фармпрепаратов, fuzzy + AI-вердикты). Решение юзера 01.08,
> Route_API_1C отклонён явно. Их оракул — `pytest tests/` над функциями, а продукт — веб-морда,
> куда человек грузит файл и получает свод: ровно тот разрыв, ради которого L2 и придуман.
> Детали проекта — в разделе «Проект для живого smoke» чекпоинта. **На правки в нём нужно
> разрешение юзера — спроси до начала.**
>
> Сначала проверь по `.planning/CHECKPOINT-2026-07-29-elt-quality-regression-audit.md`, какие из
> трёх регрессов (OCR, счётчик токенов, Visicom) вообще ЖИВУТ в этом проекте: Visicom — геосервис,
> т.е. скорее Route_API_1C. Если регрессы размазаны по двум проектам — сказать это юзеру и
> согласовать, что считать «3/3», а не подгонять.
>
> Слайсом T018 в `specs/011-elt-v3-gate/tasks.md`: (1) `smoke` в `harness.json` — сначала у них,
> потом у нас; (2) `smoke` в fleet-пути (`tools/fleet/gate.js` его сейчас НЕ вызывает — только
> `elt.js:330` в `runOracle`, т.е. fleet-воркеры слой не проходят); (3) регрессы как
> воспроизводимые кейсы + пруф, что smoke их ловит, а юнит-оракул нет.
> Гейт одной фоновой цепочкой, судья `--provider claude --model sonnet`.
>
> Учти: AC12 в `spec.md` 011 называет проектом приёмки `Route_API_1C`. Если T014 тоже вести в
> «Задача фузи музи» — это правка спеки и переутверждение (`specApproval: true`); спроси.
> После smoke: judge-bench на `claude/sonnet` + исторические блоки в набор (сейчас 3 pass-кейса,
> FPR неизмерим), затем T014 (приёмка) и T015 (замер).
