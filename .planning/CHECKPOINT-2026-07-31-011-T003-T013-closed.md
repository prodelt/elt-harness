# CHECKPOINT 2026-07-31 (5) — 011: T003–T013 закрыты, осталось T014 + T015

Предыдущий: `.planning/CHECKPOINT-2026-07-31-010-closed-D-011-T002.md`.
Ветка `feature/judge-bench-parallel-oracle`, дерево чистое.

## Закрыто в этой сессии — 11 слайсов подряд, каждый живым гейтом

| слайс | коммит | что |
|---|---|---|
| T003 | `8440a2f` | Проводка L0 в гейт: нет триггеров → судья не зовётся, `l0-clean` в run-log |
| T004 | `ed3a5ce` | Третий исход `inconclusive` + очередь `.harness/review-queue.jsonl` |
| T005 | `b8741de` | `elt review` / `elt review close`, длина очереди в `doctor` |
| T006 | `fe78b4a` | L1: impact-выборка тест-файлов (`oracleSelect: impact`) |
| T007 | `d5d66ea` | red-proof: изменённый/ослабленный/удалённый тест |
| T008 | `57f952c` | `tools/elt-mutate.js` — мутатор по изменённым строкам |
| T009 | `7f4d2ab` | ctx7-пруф + L0-триггер `external-import-no-ctx7` |
| T010 | `9a0ee20` | L2 smoke (`harness.json.smoke`) |
| T011 | `304f2ec` | Люк самозаверения удалён целиком |
| T012 | `cafedd7` | Авто-чекпоинт молчит во время гейта |
| T013 | `4d56f01` | Скилл `elt` описывает новый гейт; контракт-тест |

Судья на всех — `claude/sonnet` (agy умер `spawn ENAMETOOLONG` на первом же диффе T003, за
0.006 c — известный дефект, обход из памяти проекта). Каждый слайс: зелёный оракул → судья
`pass` → red-proof `red` → commit.

## Главный измеренный результат — оракул перестал быть 90% слайса

`oracleSelect: impact` включён на этом репо. Живые прогоны гейта:

| слайс | выборка | оракул |
|---|---|---|
| T006 | 64/64 (fallback: `.harness/harness.json` в диффе) | 157 c |
| T007 | **18/64** | **95 c** |
| T008 | **1/65** | **1.6 c** |
| T013 | **19/67** | **109 c** |
| T011 | 54/67 (тронут `elt.js` — хаб) | 178 c |

Было 150–213 c на КАЖДОМ слайсе безусловно.

## Что оказалось не так, как записано в спеке (важнее, чем сделанное)

- **`codegraph affected` в этом репо возвращает ПУСТО для каждого файла** (43 import-узла на
  288 файлов — рёбра `require()` не проиндексированы). Один codegraph как источник дал бы
  «тестов не задето» → оракул не гонял бы ничего и был бы зелёным всегда. Поэтому источников
  два: обратный скан упоминаний по диску (точный для CommonJS) + codegraph как добавка.
- **T007: премиса задачи не подтвердилась.** `red-proof` НИКОГДА не пропускал изменённые
  тесты — `git status --porcelain` даёт `M` вместе с `A`/`??`. Зато рядом нашёлся настоящий
  дефект: удаление тест-файла роняло весь слой исключением ENOENT.
- **T011: найден живой дефект dogfood'ом.** `elt judge run` писал дескриптор в литеральный
  `.git/`, а в fleet-worktree `.git` — ФАЙЛ-указатель → `mkdirSync` ENOTDIR. Раньше не всплывало,
  потому что fleet в `judge run` не заходил. Поймано 12 упавшими тестами fleet.
- **T013: структурный дефект red-proof.** У слайса, чей продукт лежит ВНЕ репо, откат к baseHead
  не откатывает предмет проверки → тест зелен на базе ВСЕГДА, и `green` означал «блокирую по
  невозможности». Теперь набор целиком из внешних предметов → `skipped: external-subject`.
- **T011: отступление от спеки.** «Fleet пишет proof in-process» не сделано: in-process требует
  вытащить из `elt.js` всю привязку задач (завязана на argv). Сделано через тот же путь
  `elt judge run` с мостом-повтором `tools/judge-replay.js` — гарантия та же (forgeable-флага в
  CLI нет), блast-radius на порядок меньше.
- **T008: мутатор НЕ проведён в гейт** намеренно — `[files:]` этого не объявлял, а цена слоя
  (прогоны тестов × выборка) требует отдельного замера.

## ОСТАЛОСЬ В РОАДМАПЕ — 2 слайса

- **T014 (приёмка, AC12) — начат и упёрся в разрешение.** Живой блок в
  `C:/Ametrin projects/Route_API_1C` через глобальный резолв моста. Сделано: `node tools/sync-bin.js`
  разложил замыкание (10 файлов) в `~/.claude/bin/judge/`; их оракул проверен живьём — **164
  pytest-теста, зелёный за 5 c**; их дерево чистое. **Блокер:** правка файла в том проекте
  (внести нарушение в `tests/test_coord_parser.py` + временный `- [ ] T900` в
  `specs/004-*/tasks.md`) дважды отклонена auto-mode классификатором — нужно разрешение
  пользователя на запись в `C:/Ametrin projects/Route_API_1C`.
  План после разрешения: ослабить один assert → `elt judge run --task T900 --provider agy`
  (НЕ claude, R3) → ждать `block`; затем чистый слайс (новый модуль + его тест) → ждать
  `pass`/`l0-clean`; пруф — записи в их `.git/elt/run-log.jsonl`; после — откатить обе правки.
  У них в `harness.json` есть `judge.verify: codex` — двойной судья там ещё живёт.
- **T015 (AC13)** — итоговый замер против baseline 77% / 100% / 185 c: block-rate на
  judge-bench, доля слайсов, дошедших до L3, доля `inconclusive`, время гейта p50/p90.
  Отчёт `.planning/GATE-011-VERDICT.md` + `tools/gate-verdict.test.js`.
  **Часть данных уже есть в этом файле** (таблица выборки/времени выше, 11 слайсов = 11 точек:
  block-rate 1/12 прогонов, единственный блок — тот самый структурный `red-proof:green`).

## Гочты, подтверждённые живьём

- `node --test <файл>` в этом репо ВИСНЕТ на части файлов; репо-раннер гоняет плоским `node`
  (`testCmd: "node"` в harness.json). Проверять свои тесты надо так же, иначе ловишь фантомное
  зависание (потерял на этом ~5 минут на T012).
- `tools/elt.js` ≡ `~/.claude/bin/elt.js` синхронить руками ПОСЛЕ каждой правки elt.js, иначе
  `doctor.test.js` красный (ловил дважды).
- Порядок цепочки жёсткий: `spec approve → oracle → judge run → commit --skip-oracle`, в дерево
  между шагами не писать. Соблюдён на всех 11 слайсах, ни одного stale-tree.

## Resume Prompt

> Продолжаю Pipeline Setupper, ветка `feature/judge-bench-parallel-oracle`. Читай
> `.planning/CHECKPOINT-2026-07-31-011-T003-T013-closed.md`. В роадмапе 011 осталось два слайса:
> **T014** (живой блок в `C:/Ametrin projects/Route_API_1C` — нужно твоё разрешение на правку
> файлов в том проекте, план в чекпоинте) и **T015** (итоговый замер + `.planning/GATE-011-VERDICT.md`).
> Гейт гнать одной фоновой цепочкой, судья — `--provider claude --model sonnet` (agy падает
> ENAMETOOLONG).
