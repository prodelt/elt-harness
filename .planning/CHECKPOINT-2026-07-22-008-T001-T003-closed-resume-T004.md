# CHECKPOINT 2026-07-22 (закрытие сессии) — 008 усиление судьи: T001-T003 закрыты, T004 в полёте

Ветка `feature/judge-bench-parallel-oracle`. Коммиты сессии:
`7f8183b` (007 целиком), `93b0070` (спека 008), `827060b` (T001 grounding),
`135a83b` (T002 двойной судья), `36cd59e` (T003 red-proof).

## Задача сессии

Пользователь: судья `agy/gemini-3.6-flash-high` быстрый (42с), но некачественный —
галлюцинирует, может пропустить ошибки в коде. Нужно «подтверждение реальное доказанное,
что всё работает как задумано, а не абы как». Плюс — закрыть остатки роадмапа.

Решения (грилл 2026-07-22, три развилки): контур из ТРЁХ слоёв (grounding + двойной судья +
red-proof); второй судья = `codex/gpt-5.6-sol` (не ест Claude-лимит, ошибается в безопасную
сторону — на бенче единственный промах был ложный `block`); порядок — сначала 008, потом
остаток 006 драйвером уже под усиленным гейтом.

## Закрыто

**007 harness-speed — целиком** (`7f8183b`, батч T001-T003, оракул 48/48): judge-bench 9→14
кейсов; `elt-gate-e2e.test.js` — e2e РЕАЛЬНОГО гейта (настоящий `elt.js`+git, красный путь
тоже, раньше все тесты гейта были на стабах); `judge-bench-compare.js`.

**008 T001 grounding** (`827060b`, оракул 49/49): `checkGrounding` в `tools/fleet/gate.js` —
`phantom-file`/`unreviewed-file`/`no-reasons`, любой отказ форсирует `block` НЕЗАВИСИМО от
вердикта модели. Две правки против первой реализации драйвера, обе из живого прогона:
1. `phantom-file` = путь, которого НЕ существует на диске (первый вариант «не в диффе =
   фантом» дал ЛОЖНЫЙ block на честном `pass`: судья перечислил `spec.md`/`tasks.md`, которые
   ему показывают как рубрику). Регресс-тест на кейс есть.
2. `no-reasons` БЕЗУСЛОВЕН, до проверки `filesReviewed` — судья заблокировал первую версию по
   делу: чек был спрятан за `filesReviewed === null`, т.е. провайдер без structured output
   (codex/agy!) снова провёл бы `pass` с пустым reasons.
Побочно: 7 стабов в `fleet.test.js` + 1 в `doctor.test.js` печатали `{"verdict":"pass"}` без
reasons → приведены к реальному контракту судьи (ассерты НЕ ослаблены).

**008 T002 двойной судья** (`135a83b`, оракул 49/49) — закрыт драйвером.
**008 T003 red-proof** (`36cd59e`, оракул 49/49) — `tools/red-proof.js` (96 строк): worktree на
`baseHead`, удаление в `finally`+`prune`, статусы `red`/`green`/`skipped:no-new-tests|no-test-cmd`.

## В полёте на момент закрытия сессии

**T004** (проводка контракта) — драйвер писал `tools/elt.js`, `tools/judge-invoke.js`,
`tools/elt-loop.ps1`. Драйвер автономен: доработает, прогонит оракул+судью и закоммитит сам.
Проверить после: `git log --oneline -1` и `grep "^- \[X\]" specs/008-judge-strength/tasks.md`.
Если он упал на полпути — дерево останется грязным, работу доделать руками по тексту T004.

## Открытые дефекты (найдены живьём)

1. **`elt-loop.ps1:256` пишет `--reasons-json "[]"` жёстко** — обоснования судьи НЕ доходят до
   judge-proof, пруф содержательно пуст (видно в T002/T003: `"reasons": []` при живом
   судье с развёрнутым разбором). Это предмет T004 — проверить, что реально починено.
2. **Approval-гейт глушит автономный драйвер после КАЖДОГО слайса**: `elt commit` ставит `[X]`
   → `tasksHash` меняется → `stale` → следующий `elt slice next` = exit 4, драйвер стоп
   (прогон на 3 слайса закрыл 1). Обход — ручной `elt spec approve` между слайсами. Лечится
   self-renew в CLI: дифф `tasks.md` ровно `[ ]`→`[X]` → тихо перевыпустить подпись (эту
   логику SKILL.md уже разрешает агенту руками). **Слайса в плане НЕТ** — спека 008
   утверждена без него, пользователю задан вопрос (добавлять T006?) — ОТВЕТА НЕ БЫЛО.

## Дальше

1. Проверить итог T004 (см. выше).
2. **T005** — руками: прогнать judge-bench (14 кейсов) на усиленном контуре, сравнить через
   `judge-bench-compare` (recall не ниже, ложные pass не выросли), включить `judge.verify`
   (codex/gpt-5.6-sol) в `.harness/harness.json`, живой прогон на реальном слайсе.
3. Решить вопрос T006 (self-renew approval) — без него 006 придётся тыкать вручную 10 раз.
4. **006** T009-T018 (10 слайсов) драйвером под усиленным гейтом.
5. **005** T021 (Fleet ledger). T022/T023 — внешние блокеры (Ametryn A/B + pilot approval).

## Ритуал между слайсами (пока дефект №2 жив)

```powershell
node "$env:USERPROFILE\.claude\bin\elt.js" spec approve --spec specs/008-judge-strength
powershell -File tools\elt-loop.ps1 -Project . -SpecDir specs/008-judge-strength -Slices 1 -Batch 1 -JudgeProvider claude -JudgeModel sonnet
```
