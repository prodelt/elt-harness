# CHECKPOINT 2026-07-28 — 009 T010: код готов и доказан, остался ОДИН прогон гейта

**Спека:** `specs/009-elt-v3-thinking-harness` · **ветка:** `feature/judge-bench-parallel-oracle` · **HEAD:** `232b209`
**Состояние:** T010 дописан, `approve` свежий, дерево грязное намеренно. Осталось: прогнать
цепочку `oracle → judge run → commit` — и слайс закрыт. Код НЕ менять, он уже судился на pass.

## Что сделано в этой сессии (сверх прошлого чекпоинта)
Блокер «verify-судья codex трижды dead» закрыт **failover'ом verify-судьи** (юзер выбрал этот
вариант из трёх). Потом живые прогоны вскрыли ещё два дефекта — оба починены:

1. `tools/fleet/gate.js` — мёртвый verify-судья (`runOk:false`) **перевыдаётся следующему
   доступному CLI**, не совпадающему ни с первичным, ни с мёртвым, ни с воркером (`judgeExclude`).
   Мёртвая попытка остаётся в `judges[]` — след, не тихая подмена. `JUDGE_TIMEOUT_MS` = **8 мин**.
2. `tools/fleet/providers.js` — `available(provider)` переехал сюда из `fleet.js` (нужен обоим),
   `JUDGE_ALTS` — один список (`gate.JUDGE_ALTS`), не дубль. `fleet.js` шлёт `judgeExclude:[воркер]`.
3. `tools/judge-verify.test.js` — стаб расширен на все 3 CLI (незастабленный codex считался живой
   альтернативой; файл спавнил РЕАЛЬНЫЙ codex, 87с → 2.3с).
4. **`tools/elt.js` — валидатор proof (дефект, найденный ТОЛЬКО живым гейтом):** требовал вердикт
   `pass|block` от КАЖДОЙ записи `judges[]` и отвергал перевыдачу как `missing-judges` — то есть
   гейт с failover физически не мог закоммититься. Теперь вердикт спрашивается с ОТВЕТИВШИХ
   (`runOk!==false`), и хотя бы один ответивший обязателен (иначе proof из одних смертей проехал
   бы как pass). Зеркало `~/.claude/bin/elt.js` синхронизировано (`cp`), тесты 11/11.

## Доказательства (уже есть, повторять не нужно)
- Оракул **58/58** (три зелёных прогона: 180с / 192с / 299с).
- Судьи дали **pass дважды** на этом же коде:
  - `agy pass 38с` → `codex DEAD 300с` → **перевыдача** → `claude/sonnet pass 265с`;
  - `claude pass 134с` → `codex DEAD 481с` → **перевыдача** → `agy pass 103с`.
  Оба раза `redProof: red` (fails-on-base), `attested: true`. Failover — работает живьём.

## Почему слайс всё ещё не закоммичен (5 попыток, ни одна не про качество кода)
1. `elt commit --task T010` без `--spec` → связался с `specs/006` (коллизия ID). **Всегда `--spec`.**
2. `stale-tree` — хук авто-чекпоинта переписал `.planning/CHECKPOINT-*-auto.md` после фиксации пруфа.
3. `stale-tree` — я сам записал файл в дерево во время оракула цепочки (моя ошибка).
4. `judge dead` — сперва claude не уложился в 5 мин (302с), потом завис первичный `agy` (480с).
5. `missing-judges` — дефект валидатора (см. п.4 выше), уже починен.
Последняя цепочка убита на этапе оракула, до судьи.

## КОРЕНЬ «зависаний» codex НАЙДЕН И УСТРАНЁН (2026-07-28, разбор живого лога 567KB)
codex НЕ висел и НЕ ждал апрува (`approval: never` в шапке лога). Он спавнился с
`--sandbox workspace-write` и уходил **гонять оракул внутри судейского вызова**:
`exec powershell -Command 'node tools/elt-oracle-runner.js'`. Агентный CLI с правом записи
ведёт себя как исполнитель: не укладывается ни в какой таймаут (300/301/301/481/540с, DEAD 5/5)
и пишет в дерево — это же источник части `stale-tree`, которые списывались на хук.
**Фикс (в зоне T010):** `judgeDiff` → `providers.run({readOnly:true})` → codex-судья спавнится
`--sandbox read-only` (воркеру запись остаётся); плюс первым абзацем промпта судьи — запрет
запускать тесты/команды. Тест `providers.test.js` сверяет argv обеих ролей. 23/23 зелёных.
**Живьём этот фикс ещё НЕ проверен** — цепочка упала раньше судьи (см. ниже).

## FLAKE (карантин, не чинить вслепую)
Прогон оракула 2026-07-28 ~16:40 упал на `tools/context7-cli.test.js` (файл вне зоны слайса,
не трогался). Отдельный перепрогон — **зелёный** (1/1). Классический flake; до него три прогона
подряд давали 58/58. Если повторится — карантинить, а не «чинить» подгонкой.

## ДАЛЬШЕ — ровно одна команда (ничего не править!)
Всё писать в дерево ДО старта; пока цепочка идёт — не писать НИЧЕГО (иначе `stale-tree`):
```bash
node ~/.claude/bin/elt.js oracle \
 && node ~/.claude/bin/elt.js judge run --task T010 --spec specs/009-elt-v3-thinking-harness --provider claude --model sonnet \
 && node ~/.claude/bin/elt.js commit --task T010 --spec specs/009-elt-v3-thinking-harness --skip-oracle
```
`--provider claude` — потому что `agy` в конце сессии начал зависать (было 23-38с, стало 480с),
а `claude` отвечает стабильно (134-265с). Время цепочки ~18 мин (из них codex ~8 мин впустую).
Если снова `stale-tree` — повторить ту же цепочку одним вызовом, причина внешняя (хук пишет в дерево).

## Открыто после T010
- **T011** (живой прогон, закрывает спеку), затем T012/T013 (фаза F, ускорение).
- **Мёртвый ПЕРВИЧНЫЙ судья по-прежнему валит гейт сразу** — failover сделан только для verify.
  Вне зоны T010, очевидный кандидат в следующую задачу спеки.
- **codex/gpt-5.6 висит на крупном промпте** (~60K дифф): 301/301/540/300/481с. Не блокер
  (failover есть), но не диагностирован, и жрёт 8 мин таймаута на каждом гейте репо.
