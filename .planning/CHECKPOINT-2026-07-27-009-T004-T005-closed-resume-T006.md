# CHECKPOINT 2026-07-27 — 009: T004 и T005 ЗАКРЫТЫ, resume T006

## Состояние
- Ветка `feature/judge-bench-parallel-oracle`, HEAD `a46f90b`. Оракул **56/56**.
- Фаза A закрыта раньше; **фаза B: T004, T005 закрыты**, остался **T006**.
- Зеркало `~/.claude/bin/elt.js` синхронно (`36cfa9f` в репо `C:/Users/user/.claude`).

## Закрыто

### T004 park & continue — `7a1917f`, оба судьи pass
Трёхраундовый standoff (agy pass / codex block) разрешён **не переутверждением спеки, а
двумя исполняемыми тестами** вместо структурных — codex требовал именно исполнения:
1. `Park-Slice` вытаскивается из `elt-loop.ps1` и гоняется настоящим PowerShell против
   temp-репо (зонд пишется ВНЕ репо — `stash -u` унёс бы исполняемый файл; BOM обязателен).
2. Полный прогон `elt-loop.ps1` против репо с красным оракулом: T001 паркуется, петля
   берёт T002, итог `exit 1`. ~5 с.

### T005 heal видит ошибку — `a46f90b`, оба судьи pass
`elt oracle` захватывает stdout/stderr (`sh` → `{code,out}`), пишет хвост 8K с КОНЦА в
`.harness/oracle-tail.log` (игнор через общий `gitExclude`, вынесен из `writeParked`).
Драйвер: до 2 попыток self-heal (high → max), вывод оракула идёт ПЕРВОЙ секцией промпта,
impl-лог — второй; каждая попытка пишет `{result:heal, healAttempt, effort}` в run-log.

## Найдено живьём (настоящие дефекты, не придирки)
1. **maxBuffer** (нашёл codex): `spawnSync` без `maxBuffer` = 1 МБ, при превышении процесс
   УБИВАЕТСЯ → болтливый оракул отдал бы ложный провал. Поставлен 256 МБ + тест на 4 МБ вывода.
   Тот же дефект был у `spawnSync` в самом тесте — тестовый хелпер тоже получил maxBuffer.
2. **Коллизия id между спеками** (блокер гейта, чинен отдельным `30a4504`): `judge run`/
   `commit` искали `T005` по всем спекам и брали первую по алфавиту (008 раньше 009) →
   судья с ЧУЖОЙ рубрикой и, хуже, `[X]` в чужом плане. Фикс — одна строка в
   `findTaskItem` (`--spec` резолвится там же), тест `tools/elt-spec-collision.test.js`.
3. **Зеркало и дифф судьи**: судья читает внешний репо ДИФФОМ. Закоммиченное зеркало = пустой
   дифф = codex вменяет «живой CLI не изменён»; незакоммиченное с чужими правками = scope creep.
   Рабочий порядок: во внешнем репо коммитить ТОЛЬКО чужие/предыдущие правки, правку текущего
   слайса оставлять незакоммиченной до гейта, коммитить зеркало ПОСЛЕ `elt commit`.

## Гочты сессии
- Гейт требует чистого дерева по зоне слайса: `.planning/elt-system-audit-latest.md`
  (авто-вывод внешнего аудита) дважды ловился судьёй как scope creep — убирать в stash.
- Правка дерева между `elt oracle` и `judge run` = `stale oracle proof`; гонять оракул
  ИМЕННО через `node tools/elt.js oracle` (прямой запуск runner'а пруф не пишет).
- `approval.json` после `[X]` — тихий re-approve разрешён (дифф только `[ ]`→`[X]`),
  коммитить отдельным chore ДО гейта, иначе судья вменит его слайсу.
- stdout PowerShell приходит в OEM-кодировке — ассертить состояние (файлы, exit), не текст.

## ДАЛЬШЕ
**T006** — промпт имплементатора v2 (сначала разведка: codegraph_context по зоне, спека,
существующие тесты; запреты ПОСЛЕ) + JSON-заявка `{"filesChanged","testsAdded"}` в конце
слайса (почва для T009) + эффорт по тегу сложности (`[S]/[M]`→high, `[L]`→max) в
`tools/fleet/effort-policy.js`, драйвер передаёт тег.
`elt slice next` ВСЕГДА с `--spec specs/009-elt-v3-thinking-harness` — и теперь то же
самое для `judge run`/`commit` (см. дефект 2).
