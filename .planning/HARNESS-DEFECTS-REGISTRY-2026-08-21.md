# Реестр дефектов харнеса ELT — аудит field reports 2026-08-15 / 2026-08-21

> Сводка всех зафиксированных дефектов из трёх полевых отчётов, их статус, корневые причины и измеренные эффекты. Номера ссылаются на дефекты, как опубликованы в отчётах.

---

## Таблица дефектов — статус и воздействие

| Дефект | Симптом | Корневая причина | Источник (файл:строка) | Статус | Эффект |
|---|---|---|---|---|---|
| **D1** | `git worktree` пуст, оракул падает на `npx tsc`/`npm run lint`/`npm test` | `node_modules` в `.gitignore`, worktree не переносит неотслеживаемое | HARNESS-FIELD-REPORT-2026-08-15.md:32-50 | ✅ Исправлено (`tools/fleet/worktree.js`) | Fleet был полностью неработоспособен для Node-проектов до первой строки кода воркера |
| **D2** | `git worktree remove` удаляет корневой `node_modules` сквозь junction | `git worktree remove --force` входит внутрь reparse point — **уточнено 2026-08-22**: виновник именно git, а не `fs.rmSync` (на Node 24 rmSync линк только отвязывает, цель цела; проверено на чистой фикстуре: без снятия линка `git worktree remove --force` уносит маркер из КОРНЕВОГО `node_modules`) | HARNESS-FIELD-REPORT-2026-08-15.md:52-74 | ✅ Исправлено (`tools/fleet/worktree.js`), регрессия D1 | Тихая порча основного дерева, замаскирована сообщением о локе; корневой `node_modules\.bin` потерял 117 бинарей |
| **D3** | `attest.js` считает новый тест незаявленным (`undeclared-file`) | Асимметричная сверка: `hallucinated` проверяет union заявки, `undeclared` проверяет только `filesChanged`, игнорируя `testsAdded` | HARNESS-FIELD-REPORT-2026-08-15.md:76-112 | ✅ Исправлено (`tools/fleet/attest.js` + регресс-тест) | 19 отказов из 20 подряд; любой слайс с новым тестом непроходим в принципе |
| **D4** | Approval-guard смотрит рабочее дерево, слайсы читают HEAD; несинхронная подпись | `approval-guard` проверяет файл главного репо, `elt commit` в worktree читает из HEAD | HARNESS-FIELD-REPORT-2026-08-15.md:114-144 | ✅ Закрыт спекой 018 (2026-08-22) | 9 отказов в конце цепочки (после воркера+оракула+судьи); один незакоммиченный `approval.json` сжигает весь LLM-бюджет раунда |
| **D5** | `node tools/doctor.js --fleet` не проверяет готовность fleet к прогону | Команда либо не существует в целевом проекте, либо отвечает `FAIL=0` на неработоспособной конфигурации; отсутствует предпусковой диагност самого fleet | HARNESS-FIELD-REPORT-2026-08-15.md:146-159 | ❌ Не исправлено | Обязательный по контракту скилла шаг не выполним; оба дефекта D6 и D7 не показал бы ни один вариант |
| **D6** | `worktree.create()` переиспользует старую ветку `fleet/<Tid>` вместо `base` | Комментарий объявляет переиспользование фичей, но `base` молча игнорируется при `branchExists=true` | HARNESS-FIELD-REPORT-2026-08-15-run2.md:35-117 | ❌ Обойдено вручную (удаление веток) | Воспроизводит D4 полностью даже когда оператор выполнил документированный обход; worktree встаёт на старом коммите с устаревшей `approval.json` |
| **D7** | Approval становится `stale` ВНУТРИ одного прогона, гарантированно убивает слайсы после первого merge | `elt commit` меняет `tasks.md` → `tasksHash` уходит → подпись невалидна для слайсов, созданных после merge | HARNESS-FIELD-REPORT-2026-08-15-run2.md:120-196 | ✅ Закрыт спекой 018 (2026-08-22) | При `--workers 3` и 4+ открытых слайсах слайс N>3 гарантированно падает в конце цепочки; механизм, спроектированный в коде, воспроизведён в реальном прогоне (ts 2026-08-15T13:26:03) |
| **D8** | Фоновый вердикт `[bg-red/judge]` без единого судейского вывода; молчание маскируется под красный | `bg-red` используется для обеих ситуаций: (1) судья ответил и заблокировал; (2) судья не ответил вовсе | HARNESS-FIELD-REPORT-2026-08-15-run2.md:416-451 | ❌ Не исправлено | Владелец не может отличить два исхода; механизм отлова молчания (три `bg-silent` подряд) не может сработать по построению |
| **D9** | Zone-чек штрафует за файлы, которые генерирует сам процесс (package-lock.json) | Генерируемые артефакты попадают в список `changed` и считаются в zone-чеке | HARNESS-FIELD-REPORT-2026-08-15-run2.md:453-479 | ❌ Не исправлено | `diff-size` = 694 строк (порог 400) из-за `package-lock.json`; перечисление lock-файлов в `[files:]` недостижимо, т.к. они генерируются всегда |
| **D10** | `external-import-no-ctx7` считает новыми импортами пакеты, уже в `package.json` с первого коммита | Чек считает «внешним импортом» первое вхождение пакета в новых файлах диффа, а не изменение в манифесте | HARNESS-FIELD-REPORT-2026-08-15-run2.md:481-498 (подтвержд. 2026-08-15-run2.md:120; 2026-08-21-run1.md:118) | ❌ Не исправлено (4 воспроизведения) | Любой слайс с новым тест-файлом получает флаг автоматически; флаг не несёт информации и обучает игнорировать вердикт |
| **D11** | Каждый `elt commit` делает `approval.json` `stale` для следующего слайса | `elt commit` правит `tasks.md` (галочки), меняя `tasksHash`; подпись действительна на момент коммита но невалидна для СЛЕДУЮЩЕГО слайса | HARNESS-FIELD-REPORT-2026-08-15-run2.md:509-541 (подтвержд. 2026-08-21-run1.md:145-173) | ✅ Закрыт спекой 018 (2026-08-22) | На спеке из 9 задач — 8 лишних переподписей; каждая приходит после полного прогона оракула (35–53 c) впустую; `specApproval: true` на длинных спеках — источник ложных отказов |
| **D12** | `agent-browser eval --stdin` молча возвращает `null` | Mode не исполняет скрипт; регрессия в утилите, не в харнессе | HARNESS-FIELD-REPORT-2026-08-15-run2.md:568-599 | ❌ Не исправлено | Оба документированных пути передачи скрипта (--stdin и -b/--base64) закрыты одновременно; остаётся inline с проблемами экранирования PowerShell |
| **D13** | Zone-чек в batch-коммите видит зону только первой задачи | Парсер берёт только ПЕРВОЕ вхождение `[files:]` (`.match()` БЕЗ флага `g`) | HARNESS-FIELD-REPORT-2026-08-21-run1.md:23-83 (воспроизв. 2026-08-21-run1.md:188-202) | ❌ Не исправлено (2 воспроизведения) | Любой batch-коммит на N>1 задач гарантированно получает `out-of-scope` на файлах задач 2..N; режим CLI несовместим со своим же zone-чеком |
| **D14** | Порог `diff-size` не масштабируется на число задач в батче | Константа 400 строк проверяется против суммы по всем файлам всех задач | HARNESS-FIELD-REPORT-2026-08-21-run1.md:86-114 | ❌ Не исправлено | Дифф 457 строк (две задачи по 195+258 строк) падает по одному пороговому числу; тот же дифф проходит две отдельные коммиты |
| **D15** | `approval.json` не входит в `isHarnessOwned()`, хотя пишет его только харнесс | Список явно исключает `.harness/**`, но `approval.json` (генерируемый `elt spec approve`) упущен | HARNESS-FIELD-REPORT-2026-08-21-run1.md:208-242 (подтвержд. 2026-08-21-run1.md:277-282) | ✅ Закрыт спекой 018 (2026-08-22) | Сцепка: D11 обязывает переутверждать спеку → `approval.json` меняется → D15 объявляет его нарушением зоны; харнесс штрафует за файл, который генерировал сам |
| **D16** | `elt harness propose` недостижим: `require('./elt-harness-propose')` ищет в `~/.claude/bin/`, туда не разворачивается | Модули `elt-harness-propose.js` и каталог `judge-bench/` живут только в `tools/`, не включены в `elt-harness-sync-all.js` | HARNESS-FIELD-REPORT-2026-08-21-run1.md:292-315 | ❌ Не исправлено | Единственный санкционированный путь менять судью/гейт не запускается; обойдено вручную |
| **D17** | `judge-bench` предлагает `rejected` для ВСЕХ кейсов, включая базлайн (судья не стартует) | Вышестоящий дефект D18; бенч не видит, что ошибка в окружении, а не в правке | HARNESS-FIELD-REPORT-2026-08-21-run1.md:317-346 | ❌ Не исправлено (заблокировано D18) | Гейт, который должен пропускать улучшения, структурно не может пропустить ничего; отказ записывается в `learnings.jsonl` как содержательный при отсутствии измерений |
| **D18** | Резолв `claude.exe` не знает нативной раскладки; `spawn` идёт через `cmd.exe`, разваливая JSON-схему | `claudeExe()` смотрит только npm-раскладку; нативный установщик даёт PATH-результат уже в `.exe` | HARNESS-FIELD-REPORT-2026-08-21-run1.md:359-412 | ✅ Исправлено в `tools/fleet/providers.js`, ❌ НЕ в `~/.claude/bin/fleet/providers.js` | Убивает structured-output путь (judge-bench); обычные вызовы судьи работают (схему не передают); видно только на structured-output |
| **D19** | Судья считает scope creep'ом файлы, признанные `harness-owned` гейтом | `.harness/review-queue.jsonl` пишет сам харнесс; знание о владении закодировано в гейте, но не передаётся судье | HARNESS-FIELD-REPORT-2026-08-21-run1.md:431-452 | ❌ Не исправлено | Каждый слайс после разбора очереди предыдущего получает ложный упрёк; содержательная часть вердиктов верна, но смешана с шумом |
| **D20** | L0 судит документный дифф кодовыми правилами: `external-import-no-ctx7` срабатывает на листинге кода внутри `.md` | `externalImports()` сканировал ВСЕ добавленные строки диффа, не различая файл ханка; это единственный триггер, который сам выносит `block`, а L0 стоит ПЕРЕД документной дверью (016 T010) | найден живьём 2026-08-22, документный коммит архива (11 `.md`, 2955 строк): `external-import-no-ctx7` на слове `vitest` из листинга в field report | ✅ Исправлено (`tools/elt-gate-l0.js`, спека 017 T004 + регресс-тест `testDocDiffIsNotJudgedAsCode`) | Документная дверь была закрыта целиком: единственным выходом оставался ручной `git commit` мимо `run-log` — ровно тот механизм, который фаза 1 и должна убрать |
| **D21** | Документная дверь объявляет кодом любой файл с не-ASCII именем | `changedFiles()` (`tools/elt.js`) читал вывод git как есть, а git по умолчанию отдаёт такой путь в C-кавычках (`"Ð..."`): строка больше не начинается с `.planning/` и не оканчивается на `.md` | найден живьём 2026-08-22 на `Методология Agent Harness.md` — отказ `elt commit` с кавычками прямо в тексте ошибки | ✅ Исправлено (`-c core.quotepath=false`, спека 017 T005 + регресс-тест) | Вторая стена того же документного среза: дверь закрывалась на ОДНОМ файле из 30, снова выгоняя в ручной `git commit` |

---

## Измеренные числа — доказательная база

### Влияние дефектов на время цикла и LLM-бюджет

**Затраты (field report 15.08.2026, прогон 1 izi-tracker)**
- Потеряно LLM-вызовов: **28 спавнов `agy`** (19 на attest/D3, 9 на approval/D4) — без доказательства дефектов, без выхода в код
- Слайсов до merge: **1 из 9** (только T008, остальные упирались в дефекты обвязки, не качество)
- Оракул izi-tracker: **340 тестов, 209 c**; ручной прогон baseline: **36 файлов / 356 тестов, 41 c** (прогон 2)

**Лечение (field report 15.08.2026, прогон 2 после правок D1–D3)**
- Слайсов до merge: **4 из 4** (T004, T005, T006, T007) — после ручного обхода D4/D6/D7
- Отказов по вине обвязки: **0**
- Новых дефектов обвязки найдено: **2 (D6, D7)** + 1 подтверждено (D5)

**Дефекты качества гейта (прогон 2)**
- Дефектов продукта, пропущенных судьёй: **3 из 4 прочитанных диффов** (J1, J2, J3)
- Из них критических (security): **1 (J3, новая дыра в безопасности)**; уже закреплённых спекой: **2 (J1, J2)**

### Скорость и параллелизм (D7, D11, D13, D14)

**Batch-режим несовместим с гейтом (D13, D14)**
- Дифф 457 строк (две задачи) падает по пороговому числу 400
- Тот же дифф, закоммиченный одиночно, даёт 258 и 195 строк — обе под порогом
- Batch-цепочка при 3 воркерах и 4+ слайсах: слайсы 1..3 проходят, слайс 4+ гарантированно падает (D7 + D11)

**Переподписи (D11, D15)**
- Спека 002 (9 задач): при раздельных коммитах **8 лишних переподписей**, каждая после полного оракула (35–53 c)
- Спека 003 (batch T001,T002 → T003): одна переподпись потребовалась после 46 c оракула; approval был валидным ДО и после, содержание спеки неизменилось (спеки одинаковые, менялись только галочки)

### Шум в фоновой очереди (D8, D9, D10, D15, D19)

**Сигнал/шум фонового вердикта**
- Прогон 21.08.2026 (спека 003, 4 коммита):
  - Вердиктов `bg-red`: **4 записи**
  - Из них истинных срабатываний: **1** (`new-code-no-check` на T007)
  - Ложных/шума: **3** (из них D13×2, D14×1, D10×1, D15×2 — с перекрытиями)
  - Отношение сигнал/шум: **1:7**

**Каждый слайс с новым тест-файлом**
- Автоматический флаг D10 (`external-import-no-ctx7: vitest, next`)
- Пакеты уже в `package.json` с первого коммита (vitest в 48 файлах, next = сам фреймворк)
- Слайс не добавил ни одной зависимости (две миграции `.sql`)

### Оракул и собственные тесты харнеса (field report 15.08.2026)

**Pipeline Setupper oracle-runner**
- Тестов: **53 файла, первый прогон параллельно с fleet 52/53** (падение в гонке временного stop-файла)
- Самые тяжёлые: `fleet.test.js` 442.6 c, `harness-watch.test.js` 249.5 c, `elt-verify-bg.test.js` 231.8 c
- Второй прогон в изоляции: **exit 0**, 53/53

**Диагностика fleet (D5)**
- `node tools/doctor.js --fleet`: `PASS=7 WARN=326 FAIL=0` (перебор всех проектов машины)
- На izi-tracker не проверяет: готовность `node_modules` в worktree, наличие подписи в HEAD, отставших веток

---

## Открытые задачи — статус спеки 016

| Фаза | Задача | Условие | Статус |
|---|---|---|---|
| **A** | T001–T005 | Правда сигнала оракула | ✅ **ЗАКРЫТЫ** |
| **B** | T006 | Облик-данные для планировщика (`LastTaskResult`) | ✅ **ЗАКРЫТА** (регистрация на `Register-ScheduledTask -LogonType S4U`, есть обход вручную) |
| **B** | T007 | Замкнуть `benchAdded` (196 разм., но `benchAdded: 0`) | ✅ **ЗАКРЫТА** |
| **C** | T008 | `elt harness sync-all` с дифф и применением | ✅ **ЗАКРЫТА** (разворот на 12 проектов живьём) |
| **C** | T009 | run-log vs. отказ | ✅ **ЗАКРЫТА** |
| **C** | T010 | `elt commit` без `--task` для docs/chore | ✅ **ЗАКРЫТА** |
| **D** | T011 | Судья из критического пути | ✅ **ЗАКРЫТА** (синхронно только L0, судья в фон) |
| **E** | T012 | CodeGraph adoption | [ ] **Опционально** (решение пользователя) |
| **E** | T013 | Stryker-mutator вместо саморобного | [ ] **Опционально** |
| **E** | T014 | Failure-mining + auto-harness | [ ] **Опционально** |

---

## Противоречия между отчётами и STATE.md

| Противоречие | Источник | Статус |
|---|---|---|
| **Статус D1–D3 в коде vs. git** | Правки внесены в `tools/fleet/` но не закоммичены; ждут ревью владельца | HARNESS-FIELD-REPORT-2026-08-15.md:226-234 | Механическое расхождение; коммит не произойдёт без явного решения |
| **Fleet работает vs. открытые D4/D6/D7** | Прогон 2 дал 4 из 4 слайсов до merge, но только после ручного обхода D4/D6/D7 | HARNESS-FIELD-REPORT-2026-08-15-run2.md:385-407 | Нет противоречия; машина работает, но при ручном вмешательстве; автоматический путь остаётся обломан |
| **Судья работает vs. 3 дефекта продукта пропущены** | Судья дал `pass` на всех четырёх слайсах прогона 2; ручное чтение нашло J1, J2, J3 | HARNESS-FIELD-REPORT-2026-08-15-run2.md:392-406 | Истина: судья сверяет дифф со спекой, а не с реальностью; два дефекта (J1, J2) пришли из текста задачи и прошли `spec lint` |
| **Batch-режим декларирован vs. D13/D14 его разрушают** | CLI `--task T001,T002,T003` рекламирует батч-режим; L0-гейт не поддерживает | HARNESS-FIELD-REPORT-2026-08-21-run1.md:68-70 | Режим существует в CLI и оракле, но не в гейте — это не регрессия, а исходная неполнота |
| **Спеца 002 успешна vs. D11 её ломает** | STATE.md: спека 002 закрыта. Field report 2026-08-21: T010, T011, T012 требовали переподписей | Нет противоречия: спека закрыта НЕСМОТРЯ на D11, ручными обходами |

---

## Признанные архитектурные ошибки (из самих отчётов)

1. **Судья сверяет со спекой, а не с реальностью** (J1, J2)
   > «Это не промах модели-воркера — воркер исполнил задание точно. Это промах слоя, который должен сверять задание с внешней реальностью (схемой БД, семантикой Postgres).» — HARNESS-FIELD-REPORT-2026-08-15-run2.md:317-320
   - Слой сверки с внешней реальностью в харнессе отсутствует.

2. **Харнесс генерирует файлы, которые потом штрафует чеками** (D9, D11, D15)
   > «Харнесс штрафует слайс за файл, который генерировал сам, в обход зоны, объявить которую заранее невозможно» — HARNESS-FIELD-REPORT-2026-08-15-run2.md:543-557
   - Зоны `[files:]` пишут в момент планирования; переподписи и lock-файлы приходят после.

3. **Batch-режим поддержан в одном слое, не поддержан в другом** (D13, D14)
   > «Режим, который CLI сам рекламирует в `--help`, несовместим со своим же zone-чеком.» — HARNESS-FIELD-REPORT-2026-08-21-run1.md:68-70
   - Парсер `[files:]` и порог `diff-size` не масштабируются на число задач.

4. **Фоновый слой должен отличать молчание от блока, но структурно не может** (D8)
   > «Механизм, спроектированный на отлов молчания судьи, обходится тем, что молчание маскируется под красный вердикт.» — HARNESS-FIELD-REPORT-2026-08-15-run2.md:437-440
   - Единая запись `bg-red` используется для обеих ситуаций; счётчик `bg-silent` не считается без отдельного файла `health.jsonl`.

5. **Approval-guard и читатель подписи живут в разных местах (рабочее дерево vs. HEAD)** (D4, D6, D7)
   > «Свежая подпись существует, закоммичена, guard её видит — и ни один воркер её не видит.» — HARNESS-FIELD-REPORT-2026-08-15-run2.md:69-81
   - Проверка и использование разделены; нет единого источника истины для подписи.

---

## Что реально работает (с доказательством)

| Функция | Проверено | Результат | Где |
|---|---|---|---|
| `elt status`, `slice next`, `spec lint`, `spec approve`, `spec status` | Живой прогон | Работает | HARNESS-FIELD-REPORT-2026-08-15.md:162-181 |
| `elt oracle` (в worktree и главном дереве) | Живой прогон izi-tracker | 340–356 тестов, 41–209 c, exit 0 | HARNESS-FIELD-REPORT-2026-08-15.md:170 |
| Fleet оракул+судья+commit цепочка (после D1–D3, D4/D6/D7 обойдены) | 4 слайса подряд | T004, T005, T006, T007 — все до merge | HARNESS-FIELD-REPORT-2026-08-15-run2.md:385-407 |
| `fleet attest.js` (после D3) | Регресс-тест + живой | Новый тест не считается незаявленным | HARNESS-FIELD-REPORT-2026-08-15.md:77-110 |
| `fleet worktree.js` (после D1+D2) | Живой прогон | Чистая синхронизация, корневой `node_modules` цел | HARNESS-FIELD-REPORT-2026-08-15-run2.md:228-230 |
| `fleet merge.js` | Живой прогон | T008 смержен, `[X]` проставлен | HARNESS-FIELD-REPORT-2026-08-15.md:179 |
| Судья на истинном дефекте | Прогон 21.08 | `new-code-no-check` на T007 пойман верно | HARNESS-FIELD-REPORT-2026-08-21-run1.md:246-268 |
| `elt-retro-label.js` при регистрации планировщика | После T006 | `LastTaskResult=0` + запись в `learnings.jsonl` | STATE.md, T006 |

---

## Оценка CLAUDE.md и AGENTS.md

| Аспект | Находка |
|---|---|
| **Размер** | 45 строк (актуальный `CLAUDE.md`); Memory section = указатель, не журнал. Эталон: `.planning/STATE.md` живёт В ПРОЕКТЕ |
| **Инструкции** | ~12 действительных директив (Stack, Commands, Code style, Testing, Commit & PR, Gotchas, Memory как указатель) |
| **Обходы vs. правки** | Две из трёх рекомендаций (D5, D15 в готовке, J1/J2 в готовке) — это обходы дефектов, не принципиальные инструкции. Тем не менее указатель на `~/.claude/_backup-amos-full-2026-06-18/` в CLAUDE.md неактуален (AMOS-слой снят 2026-06-18) и может быть удалён |
| **Контракт скилла** | Явно не проговорена требуемая дисциплина: «владелец ОБЯЗАН читать каждый дифф руками»; сейчас написано как опция, а реально это mandatory из-за ограничений судьи |

---

## Выводы

### По очереди приоритета исправления (дороговизна эффекта)

1. **D7 → пере-подпись в merge.js** (гарантированный отказ всех слайсов >N при N воркерах)
2. **D6 → `--ff-only` до base** (восстановление после падения тихо возвращает старый approval и код)
3. **D5 → fleet doctor, живущий рядом с fleet** (предпусковой диагност самого fleet)
4. **D11 → нормализованный tasksHash** (исключить статус из подписи)
5. **D13, D14, D15 → batch-поддержка в гейте** (режим несовместим с L0)
6. **D8, D19 → отделить сигнал от шума** (различать молчание от блока, исключать harness-owned из диффа)
7. **D18 → прокатить claude.exe fix в ~/.claude/bin/** (structured-output путь мёртв до этого)
8. **J1, J2 → слой сверки с реальностью** (два дефекта из двух прочитанных диффов пришли из спеки; судья их не видит)

### Компромиссное состояние

- **Механика достаточна** для доставки кода (прогон 2: 4 из 4 слайсов до merge).
- **Качество недостаточно** (3 из 4 диффов содержали дефекты, судья их пропустил; одна — security).
- **Шум высок** (сигнал/шум 1:7 в фоновой очереди; владельца вынуждают закрывать вручную).
- **Batch-режим декларирован, но не работает** (D13/D14 гарантируют красный фон).

## Закрытие D4/D7/D11/D15 — спека 018 (2026-08-22)

Подпись спеки снята с критического пути: `approval.json` больше не сущность, подпись живёт
в трейлерах коммита (`Spec-Approved:` / `Spec-Hash:` / `Tasks-Hash:`), разбор делегирован
самому git (`%(trailers:key=...)`) — свой построчный парсер сделал бы подпись подделываемой
обычным текстом коммита.

- **D4** (подпись расходится между деревьями): закрыт архитектурно — история одна у основного
  дерева и у worktree. Регресс в `tools/approval-guard.test.js` спрашивает guard из обоих
  деревьев и требует ОДИН ответ; guard не правился ни строкой (делегирует в `elt spec status`).
- **D7/D11** (коммит роняет подпись следующего слайса): закрыт нормализацией галочек перед
  хешированием (T001) — `tasksHash` держится за СОСТАВ задач, а не за их состояние.
- **D15** (`approval.json` вне `isHarnessOwned()`): снят вместе с самим файлом.

**Живой прогон (T006, 2026-08-22):** фикстурная спека из 9 задач, все девять закрыты подряд —
`approvalCount: 1`, `commitCount: 9`, ошибок 0, ни одного отказа «спека не подписана».
До 018 то же измерение давало 9 утверждений (8 лишних, каждое после полного прогона оракула).
Команда: `node t006-live-run.js --cleanup`.

**KPI (T008, 2026-08-22, `node kpi-commit-share.js --days 14`):** доля коммитов через харнес
в ЭТОМ репозитории — строгая **62,0% (49/79)**, мягкая 67,1% (53/79). Замерщик доработан:
`spec-approve` — третий вид коммита, который создаёт сам харнес (T003), без него доля
занижалась. Сопоставимо только с однорепным замером: было 59,5%, стало 62,0%.
Сводное число по всем проектам (базлайн 28,6%, на `28dc38d` 31,4%) НЕ пересчитывалось:
недельное окно после 018 наступает 2026-08-29 — это вход в фазу 2, а не выполненный замер.

**Уточнение критерия T005 (по фоновому вердикту на `422773e`):** `grep -rn "approval.json"
tools/*.js` не пуст — но все 17 совпадений лежат в `*.test.js` и это ровно те регрессы,
которые доказывают, что файл больше не подписывает спеку (`018 T004: одинокий approval.json
больше НЕ подписывает спеку`). Критерий относится к прод-коду:
`grep -rn "approval.json" tools/*.js --exclude='*.test.js'` пуст (проверено 2026-08-22).
Удалять литерал из тестов = удалять доказательство закрытия — не делаем.


## Закрытие D9/D13/D14/D15/D19 — спека 019, батч A (2026-08-23)

Корень у D9, D15 и D19 общий и назван поимённо: **список «принадлежит харнесу» существовал в
двух копиях** — `tools/elt-gate-l0.js` и `tools/fleet/gate.js` — и копии разошлись. Копии сняты,
заведён `tools/harness-files.js`; тест `harness-files.test.js` читает исходники обоих
потребителей и краснеет, если кто-то заведёт локальный `function isHarnessOwned` снова.

- **D9** (штраф за сгенерированный `package-lock.json`): сгенерированное исключено из подсчёта
  `diff-size`. Регресс: lock на 500 строк + правка на 2 строки не даёт `diff-size`.
- **D15** (файл харнеса как нарушение зоны): `out-of-scope` считается против
  `isIgnoredForReview`, а не против узкого списка из двух путей.
- **D19** (судья не знал того, что знал гейт): в промпт судьи добавлена секция с перечислением
  таких файлов, а блок, все причины которого называют только их, понижается до `inconclusive`
  с сохранением текста причины. Понижение работает в одну сторону; смесь шума и настоящей
  причины блок не теряет (регресс есть).
- **D13** (зона батча = только первый `[files:]`): `.match()` заменён на `matchAll(...g)`,
  зона батча — объединение всех объявлений. Регресс: три задачи с разными зонами не дают
  `out-of-scope`, а чужой файл — даёт, и назван поимённо.
- **D14** (порог `diff-size` не масштабируется): порог = `порог_на_задачу × число задач`.
  Число задач считается по маркерам в тексте батча. Регресс: 810 строк на 5 задач проходят,
  те же 810 на одну — нет, а 2400 на 5 — снова нет.

Измерение шума (T005) добавлено скриптом `tools/measure-noise.js`, число и три оговорки к нему —
в `.planning/STATE.md`. Твёрдый факт замера: **5 блокирующих вердиктов из 20 вынесены по
файлам, которые пишет сам харнес** — это и есть D15/D19 в цифрах, до сегодняшней правки.

**D22 (новый, 2026-08-23): сетевой тест роняет весь оракул под параллелью.**
`tools/context7-cli.test.js` ходит в живой Context7 API. При `jobs=8` он падает
(`86/87 passed`, единственный красный), а в одиночку — зелёный (`context7-cli tests: PASS`).
Цена дефекта: три полных прогона по ~330 c впустую и трижды незакоммиченный батч.
**Корень найден 2026-08-23 и он не в сети, а в самом тесте:** стаб-раннер передавался ВТОРЫМ
позиционным аргументом (`resolveLibrary(name, successRunner(...))`), а функция ждёт его в
`options.runner`. Стаб молча игнорировался, и тест звал ЖИВОЙ `ctx7`. Починено одной строкой.
Остаётся принцип — сеть в оракуле:
механический оракул обязан быть детерминированным, иначе он не оракул. Правка (фаза 2/3):
перевести проверку на стаб, а живой вызов оставить отдельной командой `doctor --ctx7`.

**Судья заблокировал батч A по существу — и был прав (2026-08-23).** Первая редакция T001
ставила ВЕСЬ каталог `.harness/` во владения харнеса. Следствие, которое нашёл судья, а не
тесты: слайс, объявивший `[files: .harness/harness.json]` и выключивший `redProof`, получал
блок с причиной по этому файлу — а новое понижение снимало его до `inconclusive`, и слайс,
ослабляющий собственный гейт, уезжал в коммит. Дыра закрыта разделением ДВУХ границ:

| вопрос | функция | конфиг гейта |
|---|---|---|
| мог ли воркер это писать (откат worktree) | `isHarnessManaged` | НЕТ — откатывается |
| можно ли не выносить вердикт (судья, зона, объём) | `isIgnoredForReview` | НЕЛЬЗЯ — судится как код |

Регресс — не юнит, а прогон через прод-путь `judgeDiff` со стаб-судьёй (`tools/fleet/gate.test.js`):
блок по очереди разбора понижается, блок по `harness.json` выживает, смесь шума и настоящей
причины блок не теряет. Отдельный тест держит то, что границы РАЗНЫЕ намеренно.

---

# Сводка на 2026-08-24 (019 T018) — каждый номер закрыт с доказательством или объявлен открытым

Реестр выше писался по трём полевым отчётам и с тех пор не пересматривался целиком. Ниже —
статус КАЖДОГО номера на сегодня, приведённый в соответствие с живым кодом, а не с намерениями.

**Про issue-трекер.** T018 выполнен после создания приватного репозитория
`prodelt/elt-harness`: остаток заведён через `gh issue` с меткой `harness-defect` —
[D12 #1](https://github.com/prodelt/elt-harness/issues/1) и
[D24 #2](https://github.com/prodelt/elt-harness/issues/2). Новые расхождения сначала идут в
`.elt/ledger.jsonl` (`/elt-defects`); пять записей одного правила поднимают его на разбор.

| № | статус | доказательство / причина |
|---|---|---|
| **D1** | ✅ снят вместе с подсистемой | жил в `tools/fleet/worktree.js`; `fleet/` удалён целиком (019/T006) |
| **D2** | ✅ снят вместе с подсистемой | там же; корень уточнён (виновник `git worktree remove --force`, не `fs.rmSync`) и записан в истории |
| **D3** | ✅ снят вместе с подсистемой | `attest.js` жил в `fleet/`; сверка заявки переехала в `judge-core.js` |
| **D4** | ✅ закрыт | спека 018: подпись — трейлеры коммита, одна история для основного дерева и worktree |
| **D5** | ✅ снят вместе с подсистемой | `doctor --fleet` больше нет; диагностика плагина — `node bin/doctor.js` |
| **D6** | ✅ снят вместе с подсистемой | `worktree.create()` удалён с `fleet/` |
| **D7** | ✅ закрыт | спека 018: нормализация галочек в `tasksHash` |
| **D8** | ✅ закрыт | молчание фона отличимо от блока: `harness-watch.js:193` пишет `kind: "bg-silent"` в `.harness/health.jsonl`, отдельно от `bg-red` в очереди разбора |
| **D9** | ✅ закрыт | 019/T001: единый `isHarnessOwned`/`isGenerated` в `tools/harness-files.js`, регресс `testDiffSizeIgnoresGeneratedFiles` |
| **D10** | ✅ закрыт | 019/T018: «новый внешний импорт» сверяется с манифестом НА БАЗЕ (`knownPackagesAtBase`, `git show HEAD:package.json`). Регрессы держат обе стороны; лексер отдельно отличает import/require в коде от текста внутри строковой фикстуры |
| **D11** | ✅ закрыт | commit `6858fe0`; `node --test tools/elt-spec-approval.test.js tools/elt-spec-trailer.test.js` держит `[ ] → [X]` без stale и изменение текста задачи со stale; живой T006: 9 задач, `approvalCount: 1`, `commitCount: 9` |
| **D12** | ⚠️ **не наш** | [issue #1](https://github.com/prodelt/elt-harness/issues/1): дефект `agent-browser eval --stdin`, стороннего инструмента; обход — inline-скрипт |
| **D13** | ✅ закрыт | 019/T002: зона батча читает ВСЕ вхождения `[files:]` |
| **D14** | ✅ закрыт | 019/T002: порог `diff-size` масштабируется на число задач батча |
| **D15** | ✅ закрыт | спека 018 (`approval.json` удалён как сущность) + 019/T001 |
| **D16** | ✅ снят вместе с подсистемой | `elt harness propose` удалён (019/T008); эволюцию гейта ведёт журнал `.elt/ledger.jsonl` |
| **D17** | ✅ снят вместе с подсистемой | был заблокирован D16/D18; `judge-bench` остался, но гейт на правку судьи снят |
| **D18** | ✅ закрыт полностью | фикс `.exe` fast-path в `tools/providers.js` (017/T001). Вторая половина («не в `~/.claude/bin/fleet/providers.js`») исчезла с deploy-копией (019/T015): копии больше нет |
| **D19** | ✅ закрыт | 019/T001: знание о владении доезжает до судьи, блок только на владениях понижается до `inconclusive` |
| **D20** | ✅ закрыт | 017/T004: `externalImports` различает файл ханка, документный дифф не судится кодовыми правилами |
| **D21** | ✅ закрыт | 017/T005: `core.quotepath=false`, дверь не ломается о кириллицу в имени |
| **D22** | ✅ закрыт | commit `5a9bf7a`; `tools/context7-cli.test.js:59` передаёт стаб как `{ runner }`, поэтому oracle не ходит в сеть; `node tools/context7-cli.test.js` и полный oracle зелёные |
| **D23** | ✅ закрыт | 019/T022: `parseFrontmatter` нормализует переводы строк; пять линз грузятся с CRLF. Найден фоновой верификацией, не тестом |
| **D24** | ⚠️ **открыт, не блокирующий** | [issue #2](https://github.com/prodelt/elt-harness/issues/2): `tools/elt-checkpoint.test.js` зависает под `node --test` на Linux (WSL Ubuntu, node 24), но проходит под `node <file>`. Production oracle использует второй путь |

**Блокирующих открытых: ноль.** Открытых всего два, и оба помечены выше с причиной: D12 — чужой
инструмент, D24 — расхождение двух способов запуска теста, не затрагивающее исполняемый путь.

## Признанные архитектурные ошибки — что с ними стало

1. **«Судья сверяет со спекой, а не с реальностью» (J1, J2).** Не закрыто и не объявлено
   закрытым. Слой сверки с внешней реальностью в харнесе по-прежнему отсутствует; частичное
   смягчение — пять линз ревью (`/elt-verify`), которые читают код, а не задание.
2. **«Харнес штрафует за файлы, которые генерирует сам» (D9, D11, D15).** Закрыто одним
   списком владения (`tools/harness-files.js`).
3. **«Батч поддержан в одном слое, не поддержан в другом» (D13, D14).** Закрыто.
4. **«Фон не может отличить молчание от блока» (D8).** Закрыто (`bg-silent` в `health.jsonl`).
5. **«Проверка подписи и её использование живут в разных местах» (D4, D6, D7).** Закрыто
   архитектурно: единственный источник — история git.
